# 이해를 후일로 미루지 않기

## $\mathbf P^1$, $\mathbf C/\Lambda$, $\mathrm{Gr}(2,4)$에서 ring을 실제로 쓰는 세 계산

> 내 이해를 후일로 미루지 않는 습관, 좋은 질문을 던지는 습관, 토이 모델에 대한 구체적 관찰 등이 수반 되어야 합니다. 익숙한 것들에 대해서 실질적인 질문을 멈추고 본적 없는 정보량에 대해서 느낌을 잡으려는 식으로 추상적 대상들을 대하시는 경우들 중에 수학자의 길을 꿈꾸신다면 한명의 수학자로서 진지하게 말하건대 전문적인 수학을 하는 길을 꺾으시길 진심으로 권합니다. 그러한 경우가 학위를 하는 것은 해당 개인에게 불행이며 학계에도 유익하지 않습니다.

이 문장은 제공된 자료를 비평하라는 지시가 아니다. **이 노트 자체를 검사하는 기준**이다. 이전 문서는 이 기준을 어겼다. `Spec`, `Proj`, line bundle, section ring, base change라는 말을 늘렸지만, 익숙한 세 대상에서 무엇을 실제로 계산했는지는 충분히 내려가지 않았다.

> **ASSERTION.** 이 노트에서는 추상 명사를 쓴 직후 반드시 다음 네 가지를 적는다.
>
> 1. 손으로 계산하는 대상.
> 2. 왜 그 대상을 골랐는지.
> 3. 일부를 버리면 정확히 무엇이 실패하는지.
> 4. 계산이 실제로 산출한 결론.

---

# 0. 자료가 준 지도와 이 노트가 갚아야 할 계산

제공된 「추상대수에서 대수기하로」는 다음 사전을 출발점으로 둔다.

$$
A\longleftrightarrow \operatorname{Spec}A,
$$

$$
A/I\longleftrightarrow V(I),
$$

$$
A_f\longleftrightarrow D(f),
$$

$$
A\otimes_R B
\longleftrightarrow
\operatorname{Spec}A\times_{\operatorname{Spec}R}\operatorname{Spec}B.
$$

그리고 두 익숙한 대상을 제시한다.

$$
S^2\simeq \mathbf P^1(\mathbf C),
$$

$$
\mathbf C/\Lambda\simeq E(\mathbf C).
$$

이 사전은 이번 노트의 **입력**이다. 하지만 사전을 읽었다고 공간을 ring으로 관찰한 것은 아니다. 이번 노트가 실제로 갚아야 하는 계산은 다음 세 개다.

1. $\mathbf P^1$에서 localization이 어떻게 affine coordinate $z$를 실제로 만드는가.
2. $\mathbf C/\Lambda$에서 왜 $1,\wp,\wp'$를 골라야 하며 cubic relation이 어디서 나오는가.
3. $\mathrm{Gr}(2,4)$에서 projector가 이미 평면을 완전히 기록하는데도 왜 maximal minor와 Plücker ring을 쓰는가.

마지막에 계산되는 것은 ring 자체가 아니라 **선택한 관측량들의 defining ideal**이다. Ring은 그보다 앞에서 이미 함수의 덧셈, 곱셈, localization, quotient를 수행하고 있다.

---

# I. 이미 이해한 POU 계산을 정확히 기준점으로 삼기

$S^2=U_N\cup U_S$이고 overlap 위의 closed $1$-form을 $\alpha$라 하자.

$$
d\alpha=0.
$$

partition of unity

$$
\rho_N+\rho_S=1
$$

을 잡고

$$
A_N=-\rho_S\alpha,
\qquad
A_S=\rho_N\alpha
$$

라고 두면 overlap에서

$$
A_S-A_N=\alpha.
$$

또

$$
dA_N=dA_S
$$

이므로 두 local $2$-form은 하나의 global $2$-form으로 붙는다.

이 계산에서 실제로 관찰한 것은 다음뿐이다.

- overlap의 데이터가 있다.
- 각 chart의 데이터로 분해한다.
- compatibility를 확인한다.
- global object를 얻는다.

Line bundle section에서도 같은 모양의 gluing equation이 나온다.

$$
e_\infty=g\,e_0,
$$

$$
s=f_0e_0=f_\infty e_\infty,
$$

따라서

$$
f_0=g f_\infty.
$$

하지만 여기서 곧바로 `ring은 공간이다`가 나오지는 않는다. POU 계산이 주는 것은 **local-to-global gluing의 한 실물**이다. `Proj`, localization, section coordinates는 아래에서 별도로 계산해야 한다.

---

# II. 첫 계산: $\mathbf P^1$에서 ring이 실제로 하는 일

## 1. $\operatorname{Proj}$의 chart를 localization으로 직접 꺼내기

graded ring을

$$
S=\mathbf C[X,Y],
\qquad
\deg X=\deg Y=1
$$

로 둔다.

$$
\mathbf P^1=\operatorname{Proj}S.
$$

$X\neq0$인 chart를 보려면 $X$를 invert한다.

$$
S_X=\mathbf C[X,X^{-1},Y].
$$

projective chart의 coordinate ring은 $S_X$ 전체가 아니라 degree-$0$ 부분이다. 이를 실제로 계산하자.

$S_X$의 degree-$0$ homogeneous 원소는

$$
\frac{F(X,Y)}{X^m}
$$

꼴로 쓸 수 있고, 여기서 $F$는 degree $m$ homogeneous polynomial이다. 따라서

$$
F(X,Y)=\sum_{j=0}^{m}c_jX^{m-j}Y^j.
$$

분모로 나누면

$$
\frac{F(X,Y)}{X^m}
=
\sum_{j=0}^{m}c_j\left(\frac YX\right)^j.
$$

그러므로

$$
(S_X)_0
=
\mathbf C\left[\frac YX\right].
$$

이제

$$
z=\frac YX
$$

라고 쓰면

$$
(S_X)_0=\mathbf C[z].
$$

즉

$$
D_+(X)\simeq\operatorname{Spec}\mathbf C[z].
$$

똑같이 $Y\neq0$인 chart에서는

$$
w=\frac XY,
$$

$$
D_+(Y)\simeq\operatorname{Spec}\mathbf C[w].
$$

overlap에서는 $X$와 $Y$가 모두 nonzero이므로

$$
w=z^{-1},
$$

$$
\mathbf C[z,z^{-1}]\simeq\mathbf C[w,w^{-1}].
$$

**여기서 localization이 한 실제 일**은 명확하다. $X\neq0$인 영역을 골랐고, 그 영역에서 허용되는 ratio $Y/X$를 정규함수로 만들었다.

## 2. $\mathcal O(2)$의 global sections를 두 chart에서 전수조사하기

두 chart의 local frame을 $e_0,e_\infty$라 하고

$$
e_\infty=z^2e_0
$$

로 붙인다. section을

$$
s=f_0(z)e_0=f_\infty(w)e_\infty
$$

라고 쓰면

$$
f_\infty(w)=w^2f_0(1/w).
$$

$U_0\simeq\mathbf C$에서 $f_0$는 entire function이므로

$$
f_0(z)=\sum_{n=0}^{\infty}a_nz^n.
$$

따라서

$$
f_\infty(w)
=
\sum_{n=0}^{\infty}a_nw^{2-n}.
$$

$w=0$에서 holomorphic이려면 음의 지수가 없어야 한다. 따라서

$$
a_n=0
\qquad
(n\ge3).
$$

결국

$$
f_0(z)=a_0+a_1z+a_2z^2.
$$

따라서

$$
H^0(\mathbf P^1,\mathcal O(2))
=
\operatorname{span}\{X^2,XY,Y^2\}.
$$

이 계산이 먼저다. `complete linear system`이라는 말은 이 세 방향을 실제로 얻은 뒤 붙인다.

## 3. 왜 $X^2,XY,Y^2$를 전부 고르는가

$X^2$와 $Y^2$만 고른 map을 보자.

$$
[X:Y]\longmapsto[X^2:Y^2].
$$

그러면

$$
[X:Y]
\quad\text{와}\quad
[X:-Y]
$$

가 같은 image로 간다. 두 section만으로는 점을 분리하지 못한다.

$XY$를 추가하면

$$
\nu_2([X:Y])=[X^2:XY:Y^2].
$$

$X\neq0$인 chart에서

$$
\frac{XY}{X^2}=\frac YX=z.
$$

따라서 image에서 원래 $z$를 회수한다. $X=0$인 점은 유일하게

$$
[0:0:1]
$$

로 간다. 즉 세 section 전체는 점을 분리한다.

## 4. tangent direction도 실제로 분리하는가

$X\neq0$ chart에서

$$
z\longmapsto(u,v)=(z,z^2).
$$

미분하면

$$
\frac{d(u,v)}{dz}=(1,2z).
$$

첫 성분이 항상 $1$이므로 differential은 사라지지 않는다.

무한대 chart에서는 $w=X/Y$를 쓰고

$$
[w^2:w:1]
$$

로 나타난다. affine coordinates는

$$
(\widetilde u,\widetilde v)=(w^2,w)
$$

이고

$$
\frac{d(\widetilde u,\widetilde v)}{dw}=(2w,1).
$$

$w=0$에서도 두 번째 성분이 $1$이다. 따라서 tangent direction도 잃지 않는다.

## 5. relation을 모르는 상태에서 계산하기

formal coordinates를 $Z_0,Z_1,Z_2$라 하고

$$
\Phi:
\mathbf C[Z_0,Z_1,Z_2]
\longrightarrow
\mathbf C[X,Y]
$$

를

$$
Z_0\longmapsto X^2,
\qquad
Z_1\longmapsto XY,
\qquad
Z_2\longmapsto Y^2
$$

로 정의한다.

바로 확인되는 relation은

$$
Z_0Z_2-Z_1^2.
$$

실제로

$$
X^2Y^2-(XY)^2=0.
$$

이 relation 외에 더 있는지 확인하자. quotient에서

$$
Z_1^2=Z_0Z_2
$$

를 사용하면 모든 monomial을 다음 두 꼴 중 하나로 줄일 수 있다.

$$
Z_0^aZ_2^c,
$$

$$
Z_0^aZ_1Z_2^c.
$$

이들의 image는 각각

$$
X^{2a}Y^{2c},
$$

$$
X^{2a+1}Y^{2c+1}.
$$

서로 다른 $(a,c)$는 서로 다른 monomial로 간다. 따라서 추가 relation은 없다.

$$
\ker\Phi=(Z_0Z_2-Z_1^2).
$$

## 6. quotient ring에서 원래 chart를 다시 복원하기

conic의 homogeneous coordinate ring을

$$
R=
\frac{\mathbf C[Z_0,Z_1,Z_2]}
{(Z_0Z_2-Z_1^2)}
$$

라고 하자.

$Z_0\neq0$인 chart에서 degree-$0$ ratios를

$$
u=rac{Z_1}{Z_0},
\qquad
v=rac{Z_2}{Z_0}
$$

로 둔다. relation을 $Z_0^2$로 나누면

$$
v-u^2=0.
$$

따라서 이 chart의 coordinate ring은

$$
\frac{\mathbf C[u,v]}{(v-u^2)}
\simeq
\mathbf C[u].
$$

그리고 $u=z$다. 즉 quotient ring으로 conic을 만든 뒤 localization을 하면 원래 affine chart $\mathbf C[z]$가 정확히 돌아온다.

이제야 다음 문장이 계산으로 정당화된다.

- quotient는 relation을 강제한다.
- localization은 chart를 꺼낸다.
- graded ring은 projective scaling을 기억한다.

---

# III. 둘째 계산: $\mathbf C/\Lambda$에서 왜 $1,\wp,\wp'$인가

## 7. 자료에서 주어진 것과 아직 계산하지 않은 것

자료는 격자 $\Lambda$에 대해

$$
E=\mathbf C/\Lambda
$$

를 만들고, periodic meromorphic functions $\wp,\wp'$가

$$
(\wp')^2=4\wp^3-g_2\wp-g_3
$$

을 만족한다고 제시한다.

여기서 미뤄서는 안 되는 질문은 다음이다.

1. 왜 $\wp$ 하나가 아니라 $\wp'$까지 필요한가.
2. 왜 이 둘이 유한한 section system을 이루는가.
3. 왜 relation의 첫 형태가 quadratic 대 cubic인가.

## 8. 허용할 pole을 먼저 정한다

원점의 class를

$$
O=0\pmod\Lambda
$$

라고 하자. $O$에서만 pole을 허용하고 pole order가 $n$ 이하인 meromorphic functions를

$$
L(nO)
$$

라고 쓰자.

이것은 line bundle $\mathcal O(nO)$의 global section space다. 여기서 line bundle이라는 말이 하는 실제 일은 **pole의 위치와 최대 차수를 고정하는 것**이다.

## 9. simple pole 하나는 왜 불가능한가

$f$가 elliptic function이고 $O$에서만 simple pole을 가진다고 하자. 그러면 meromorphic $1$-form

$$
f(z)\,dz
$$

도 $O$에서만 nonzero residue를 가진다.

하지만 compact Riemann surface 위 meromorphic $1$-form의 residue 합은 $0$이다. pole이 하나뿐이면 그 residue는 $0$이어야 하고, 그러면 simple pole이 아니다.

따라서

$$
L(O)=\operatorname{span}\{1\}.
$$

## 10. double pole까지 허용하면 $1,\wp$가 나온다

Weierstrass function의 원점 근방 전개는

$$
\wp(z)=\frac1{z^2}+O(z^2).
$$

$f\in L(2O)$라 하자. Laurent expansion을

$$
f(z)=a_{-2}z^{-2}+a_{-1}z^{-1}+a_0+a_1z+\cdots
$$

라고 쓴다.

residue theorem에 의해

$$
a_{-1}=0.
$$

이제

$$
f-a_{-2}\wp
$$

는 pole이 없다. compact Riemann surface 위 holomorphic function은 상수이므로

$$
f=a_0+a_{-2}\wp.
$$

따라서

$$
L(2O)=\operatorname{span}\{1,\wp\}.
$$

## 11. triple pole까지 허용하면 $\wp'$가 추가된다

미분하면

$$
\wp'(z)=-\frac2{z^3}+O(z).
$$

$f\in L(3O)$라 하고

$$
f(z)=a_{-3}z^{-3}+a_{-2}z^{-2}+a_{-1}z^{-1}+\cdots
$$

라고 쓰자.

다시 residue theorem으로

$$
a_{-1}=0.
$$

먼저

$$
f+\frac{a_{-3}}2\wp'
$$

를 취하면 $z^{-3}$ 항이 사라진다. 이어서 $z^{-2}$ 항을 $\wp$의 배수로 제거하면 pole이 전부 사라진다. 남는 것은 상수다.

따라서

$$
L(3O)=\operatorname{span}\{1,\wp,\wp'\}.
$$

이제 $1,\wp,\wp'$는 갑자기 고른 세 함수가 아니다. $O$에서 triple pole까지 허용했을 때 나오는 complete section system이다.

## 12. 왜 $\wp$ 하나로는 점을 분리하지 못하는가

격자합의 대칭성에서

$$
\wp(-z)=\wp(z).
$$

따라서 $\wp$만 보면

$$
z
\quad\text{와}\quad
-z
$$

를 구별하지 못한다.

반면

$$
\wp'(-z)=-\wp'(z).
$$

따라서 $\wp'$가 부호 정보를 복원한다.

조금 더 정확히 말하면, meromorphic function

$$
u\longmapsto\wp(u)-\wp(z)
$$

는 $O$에서 double pole을 가진다. compact curve에서 zero의 총 차수와 pole의 총 차수가 같다는 사실을 사용하면 zero는 총 두 개다. 이미 $u=z$와 $u=-z$가 zero이므로 일반 fiber는

$$
\{z,-z\}
$$

이다. $\wp'$까지 같은 경우에는 부호가 일치해야 한다. $z=-z$인 $2$-torsion에서는 둘이 애초에 같은 torus point다.

## 13. cubic relation을 Laurent expansion으로 실제 확인하기

표준 전개를 다음 차수까지 쓴다.

$$
\wp(z)
=
\frac1{z^2}
+
\frac{g_2}{20}z^2
+
\frac{g_3}{28}z^4
+
O(z^6),
$$

$$
\wp'(z)
=
-\frac2{z^3}
+
\frac{g_2}{10}z
+
\frac{g_3}{7}z^3
+
O(z^5).
$$

먼저 제곱한다.

$$
(\wp')^2
=
\frac4{z^6}
-
\frac{2g_2}{5}\frac1{z^2}
-
\frac{4g_3}{7}
+
O(z^2).
$$

다음으로 세제곱한다.

$$
4\wp^3
=
\frac4{z^6}
+
\frac{3g_2}{5}\frac1{z^2}
+
\frac{3g_3}{7}
+
O(z^2).
$$

따라서

$$
(\wp')^2-4\wp^3
=
-g_2\frac1{z^2}
-g_3
+
O(z^2).
$$

여기에 $g_2\wp+g_3$를 더하면 pole과 constant term이 사라진다.

$$
H(z)
=
(\wp')^2-4\wp^3+g_2\wp+g_3.
$$

$H$는 elliptic function이고 pole이 없다. 따라서 torus 위 holomorphic function이며 상수다. 위 전개에서 그 상수는 $0$이다.

$$
(\wp')^2=4\wp^3-g_2\wp-g_3.
$$

relation의 차수도 pole order에서 미리 보인다.

$$
\operatorname{ord}_O(\wp)=-2,
\qquad
\operatorname{ord}_O(\wp')=-3.
$$

처음으로 최대 pole order가 맞는 비선형 monomial은

$$
(\wp')^2
\quad\text{와}\quad
\wp^3
$$

이고 둘 다 pole order $6$을 가진다.

## 14. projective cubic으로 올리기

projective coordinates의 순서를

$$
[Z:X:Y]=[1:\wp:\wp']
$$

로 잡는다. 그러면 relation은

$$
Y^2Z=4X^3-g_2XZ^2-g_3Z^3.
$$

$z\to0$일 때

$$
[1:\wp(z):\wp'(z)]
\sim
[z^3:z:-2],
$$

따라서 원점 $O$는

$$
[0:0:1]
$$

로 간다. 표준 좌표순서 $[X:Y:Z]$에서는 같은 점이 $[0:1:0]$이다.

여기까지 계산한 뒤에야

$$
E\hookrightarrow\mathbf P^2
$$

라는 문장을 쓴다.

### 이번 절에서 아직 입력으로 남겨 둔 것

다음은 이 노트에서 새로 증명했다고 주장하지 않는다.

- $\wp$의 lattice periodicity.
- compact Riemann surface에서 divisor의 degree가 $0$이라는 정리.
- discriminant가 nonzero일 때 cubic이 smooth하다는 판정.
- 위 map이 biholomorphism이라는 전체 uniformization theorem.

무엇을 계산했고 무엇을 입력으로 썼는지 구분하지 않으면 이해를 다시 후일로 미루게 된다.

---

# IV. 셋째 계산: $\mathrm{Gr}(2,4)$에서 projector, wedge, ring

## 15. RREF chart에서 네 자유좌표를 얻기

$W\in\mathrm{Gr}(2,4)$의 basis를 두 row로 놓으면 full-rank matrix

$$
A\in M_{2\times4}(\mathbf C)
$$

를 얻는다. basis change는

$$
A\longmapsto gA,
\qquad
g\in GL(2,\mathbf C)
$$

이다.

첫 두 열의 determinant가 nonzero인 chart에서는 Gaussian elimination으로

$$
A\sim
\begin{pmatrix}
1&0&a&b\\
0&1&c&d
\end{pmatrix}.
$$

여기서 $a,b,c,d$는 독립이다. 이 chart는

$$
\mathbf A^4
$$

이다. 이 네 좌표 사이에서 relation을 찾는 것이 아니다.

## 16. projector는 이미 basis ambiguity를 완전히 제거한다

row frame convention에서 orthogonal projector는

$$
P_A=A^\dagger(AA^\dagger)^{-1}A.
$$

$A\mapsto gA$를 대입하면

$$
\begin{aligned}
P_{gA}
&=A^\dagger g^\dagger
\bigl(gAA^\dagger g^\dagger\bigr)^{-1}
gA\\
&=A^\dagger g^\dagger
(g^\dagger)^{-1}(AA^\dagger)^{-1}g^{-1}gA\\
&=P_A.
\end{aligned}
$$

또

$$
\operatorname{Im}P_A=W.
$$

따라서 projector는 점을 잃지 않는다. Plücker를 쓰는 이유는 projector가 실패해서가 아니다.

그런데 RREF matrix를 $A=(I_2\ Z)$로 쓰면

$$
P_A
=
\begin{pmatrix}I_2\\ Z^\dagger\end{pmatrix}
(I_2+ZZ^\dagger)^{-1}
\begin{pmatrix}I_2&Z\end{pmatrix}
$$

꼴이 되고 complex conjugation이 들어간다. projector는 Hermitian metric 계산에는 적합하지만 $Z$의 holomorphic polynomial coordinate는 아니다.

## 17. metric 없이 평면을 기록하는 top wedge

두 row를 $r_1,r_2$라 하자.

$$
\omega_W=r_1\wedge r_2
\in
\bigwedge^2\mathbf C^4.
$$

basis를 $g$로 바꾸면

$$
\omega_W\longmapsto\det(g)\,\omega_W.
$$

따라서 projective class

$$
[\omega_W]
$$

는 basis와 무관하다.

또

$$
W
=
\{v\in\mathbf C^4:v\wedge\omega_W=0\}.
$$

즉 wedge도 projector와 마찬가지로 원래 평면을 완전히 복원한다.

## 18. maximal minors를 직접 전개하기

RREF chart에서

$$
r_1=e_1+ae_3+be_4,
$$

$$
r_2=e_2+ce_3+de_4.
$$

wedge를 전개하면

$$
\begin{aligned}
r_1\wedge r_2
={}&e_1\wedge e_2
+c\,e_1\wedge e_3
+d\,e_1\wedge e_4\\
&-a\,e_2\wedge e_3
-b\,e_2\wedge e_4
+(ad-bc)e_3\wedge e_4.
\end{aligned}
$$

따라서 Plücker coordinates는

$$
p_{12}=1,
$$

$$
p_{13}=c,
\qquad
p_{14}=d,
$$

$$
p_{23}=-a,
\qquad
p_{24}=-b,
$$

$$
p_{34}=ad-bc.
$$

maximal minors를 고른 이유가 이제 계산으로 보인다. 이 여섯 수는 top wedge 전체의 basis coefficients다.

## 19. decomposability에서 quadratic relation 얻기

일반 $2$-vector를

$$
\omega
=
p_{12}e_{12}+p_{13}e_{13}+p_{14}e_{14}
+p_{23}e_{23}+p_{24}e_{24}+p_{34}e_{34}
$$

라고 쓰자.

평면에서 온 vector라면

$$
\omega=r_1\wedge r_2
$$

이므로

$$
\omega\wedge\omega=0.
$$

직접 전개하면

$$
\omega\wedge\omega
=
2\bigl(
p_{12}p_{34}-p_{13}p_{24}+p_{14}p_{23}
\bigr)e_{1234}.
$$

따라서

$$
p_{12}p_{34}-p_{13}p_{24}+p_{14}p_{23}=0.
$$

## 20. local kernel을 실제로 계산하기

$p_{12}=1$ chart에서 formal coordinates를

$$
x_{13},x_{14},x_{23},x_{24},x_{34}
$$

라고 둔다.

$$
\Psi:
\mathbf C[x_{13},x_{14},x_{23},x_{24},x_{34}]
\longrightarrow
\mathbf C[a,b,c,d]
$$

를

$$
x_{13}\longmapsto c,
\quad
x_{14}\longmapsto d,
\quad
x_{23}\longmapsto-a,
\quad
x_{24}\longmapsto-b,
$$

$$
x_{34}\longmapsto ad-bc
$$

로 정의한다.

다음 polynomial은 kernel에 있다.

$$
G=x_{34}-x_{13}x_{24}+x_{14}x_{23}.
$$

임의의 polynomial을 $x_{34}$에 관한 polynomial로 보고 monic polynomial $G$로 나누면

$$
F=QG+R(x_{13},x_{14},x_{23},x_{24}).
$$

$F\in\ker\Psi$이면

$$
R(c,d,-a,-b)=0.
$$

$a,b,c,d$가 독립이므로 $R=0$이다. 따라서

$$
\ker\Psi=(G).
$$

## 21. projective quotient ring에서 RREF chart를 복원하기

homogeneous coordinate ring을

$$
R_{\mathrm{Pl}}
=
\frac{
\mathbf C[p_{12},p_{13},p_{14},p_{23},p_{24},p_{34}]
}{
(p_{12}p_{34}-p_{13}p_{24}+p_{14}p_{23})
}
$$

라고 하자.

$p_{12}\neq0$인 chart에서 degree-$0$ ratios를

$$
u_{ij}=\frac{p_{ij}}{p_{12}}
$$

로 둔다. relation을 $p_{12}^2$로 나누면

$$
u_{34}-u_{13}u_{24}+u_{14}u_{23}=0.
$$

따라서 $u_{34}$를 제거할 수 있고 chart ring은

$$
\mathbf C[u_{13},u_{14},u_{23},u_{24}].
$$

RREF 좌표와의 대응은

$$
u_{13}=c,
\qquad
u_{14}=d,
\qquad
u_{23}=-a,
\qquad
u_{24}=-b.
$$

즉 projective Plücker ring을 localization하면 처음의 자유 chart

$$
\mathbf C[a,b,c,d]
$$

가 정확히 돌아온다.

## 22. base change가 실제로 보존하는 것

체의 확대 $K\subset L$을 생각하자. Plücker quadratic을

$$
P=p_{12}p_{34}-p_{13}p_{24}+p_{14}p_{23}
$$

라고 쓰면

$$
\left(
\frac{K[p_{ij}]}{(P)}
\right)
\otimes_KL
\simeq
\frac{L[p_{ij}]}{(P)}.
$$

coefficient field를 바꾸어도 같은 polynomial formula가 남는다. 이것이 projector와 다른 algebraic 장점이다. projector는 Hermitian adjoint를 요구하지만, determinant와 wedge는 field extension과 relative family에서 같은 식으로 움직인다.

---

# V. 세 토이모델에서 실제로 얻은 공통 구조

## 23. $\mathbf P^1$

선택을 조직한 것은

$$
\mathcal O(2).
$$

계산으로 얻은 sections는

$$
X^2,XY,Y^2.
$$

일부를 버렸을 때의 실패는

$$
[X:Y]\mapsto[X^2:Y^2]
$$

가 두 점을 합친다는 것이다.

마지막 relation은

$$
Z_0Z_2-Z_1^2=0.
$$

## 24. $\mathbf C/\Lambda$

선택을 조직한 것은

$$
\mathcal O(3O).
$$

pole cancellation으로 얻은 sections는

$$
1,\wp,\wp'.
$$

일부를 버렸을 때의 실패는

$$
\wp(z)=\wp(-z)
$$

이다.

마지막 relation은

$$
(\wp')^2=4\wp^3-g_2\wp-g_3.
$$

## 25. $\mathrm{Gr}(2,4)$

선택을 조직한 것은

$$
\det\mathcal S^*.
$$

wedge expansion으로 얻은 sections는 모든 maximal minors다.

일부를 임의로 버리면 global wedge vector 전체를 기록하지 못하고, chart 변화와 $GL(4)$ action 아래 닫힌 coordinate system도 잃는다.

마지막 relation은

$$
p_{12}p_{34}-p_{13}p_{24}+p_{14}p_{23}=0.
$$

공통 순서는 이제 slogan이 아니라 계산으로 확인되었다.

$$
(X,L)
\longrightarrow
H^0(X,L)
\longrightarrow
\Phi_L(X)
\longrightarrow
I(\Phi_L(X)).
$$

하지만 이 식을 읽을 때도 각 화살표의 계산을 생략하면 안 된다.

---

# VI. 교수님의 기준으로 이 노트를 다시 검문하기

## 26. “ring은 공간이다”라고 말하기 전

이번에 실제로 확인한 것은 다음이다.

- $\mathbf P^1$의 localization에서 $\mathbf C[z]$가 나온다.
- conic quotient ring을 다시 localization하면 같은 $\mathbf C[z]$가 나온다.
- Plücker quotient ring을 localization하면 $\mathbf C[a,b,c,d]$가 나온다.

따라서 현재 이해한 문장은 더 좁고 정확하다.

> 적절한 quotient ring과 localization을 계산하면, 이미 알고 있던 공간의 affine chart와 coordinate functions를 복원할 수 있다.

`모든 공간은 ring이다`라는 더 큰 문장은 아직 이 계산을 넘어선다.

## 27. “line bundle이 함수를 고른다”라고 말하기 전

이번에 실제로 확인한 것은 다음이다.

- $\mathcal O(2)$의 gluing equation을 풀면 $1,z,z^2$만 남는다.
- $\mathcal O(3O)$의 pole condition을 풀면 $1,\wp,\wp'$가 남는다.
- $\det\mathcal S^*$의 fiber는 $\bigwedge^2W$이고 그 coordinate evaluations가 minors다.

따라서 line bundle은 막연한 장식이 아니다. **허용할 transformation law 또는 pole order를 고정하여 finite section space를 만든다.**

## 28. “대수학이 필요하다”라고 말하기 전

이번 세 계산에서 ring이 실제로 한 일은 네 가지다.

1. polynomial observables를 더하고 곱했다.
2. kernel ideal로 image relation을 모았다.
3. quotient로 relation을 강제했다.
4. localization으로 affine chart를 회수했다.

Grassmannian에서는 여기에 base change가 추가됐다.

이 범위를 넘어 singularity, Hilbert polynomial, cohomology, derived category 같은 정보를 열거하는 것은 지금 질문에 대한 답이 아니다. 그것들은 각각 별도의 토이 계산이 필요하다.

## 29. 아직 남은 빈칸

정직하게 남겨 두어야 할 문제는 다음이다.

1. 일반 line bundle에서 언제 sections가 점을 분리하는가.
2. 언제 tangent directions까지 분리하는가.
3. ample과 very ample의 차이를 토이모델에서 어떻게 관찰하는가.
4. $\mathbf C/\Lambda$의 cubic map이 전역 biholomorphism이라는 것을 어떻게 완결하는가.
5. relative Grassmannian의 local equations가 base chart 사이에서 어떻게 붙는가.
6. Reineke tower의 각 단계에서 어떤 determinant line bundle과 curvature form이 실제로 선택되는가.

이 빈칸을 이름만 알고 넘어가면 다시 이해를 후일로 미루는 것이다.

---

# VII. 손계산 검문 문제

## 문제 1

$F(X,Y)$가 degree $m$ homogeneous일 때

$$
\frac{F(X,Y)}{X^m}
$$

가 $Y/X$의 polynomial이 됨을 coefficient 수준에서 다시 써라.

## 문제 2

$\mathcal O(3)$의 transition equation을 풀어

$$
H^0(\mathbf P^1,\mathcal O(3))
$$

의 basis를 직접 구하여라.

## 문제 3

$$
[X:Y]\mapsto[X^3:X^2Y:XY^2:Y^3]
$$

의 affine chart를 적고, quadratic relations를 최소 두 개 찾아라.

## 문제 4

$f\in L(3O)$의 Laurent principal part를

$$
a_{-3}z^{-3}+a_{-2}z^{-2}+a_{-1}z^{-1}
$$

로 놓고 residue theorem과 $\wp,\wp'$를 사용하여 모든 pole을 제거하여라.

## 문제 5

위 Laurent expansions를 직접 제곱하고 세제곱하여

$$
(\wp')^2-4\wp^3+g_2\wp+g_3
$$

의 $z^{-6}$, $z^{-2}$, constant coefficient가 모두 사라지는지 확인하여라.

## 문제 6

RREF matrix의 여섯 maximal minors를 다시 계산하고 wedge expansion의 여섯 coefficients와 하나씩 대조하여라.

## 문제 7

$p_{13}\neq0$인 Plücker chart를 골라 pivot columns $1,3$으로 RREF를 만들고, degree-$0$ localization이 다시 polynomial ring in four variables가 되는지 계산하여라.

## 문제 8

다음 두 문장의 차이를 각 예 하나로 설명하여라.

- ring은 마지막에 등장한다.
- defining ideal은 마지막에 계산된다.

---

# VIII. 결론

이 노트의 목적은 제공된 자료를 비평하는 것이 아니다. 교수님의 코멘트를 **우리의 계산 습관에 적용하는 것**이다.

그래서 이번에는 다음을 실제로 했다.

- $\mathbf P^1$에서 localization을 계산했다.
- $\mathcal O(2)$의 section을 gluing equation으로 전수조사했다.
- section 일부를 버렸을 때 점이 합쳐지는 현상을 확인했다.
- conic ideal을 계산하고 localization으로 원래 chart를 복원했다.
- torus에서 pole order를 고정해 $1,\wp,\wp'$를 얻었다.
- even/odd 성질과 Laurent expansion으로 함수 선택과 cubic relation을 확인했다.
- $\mathrm{Gr}(2,4)$에서 RREF, projector, wedge, minors, kernel, localization을 한 줄로 연결했다.

현재 정당하게 말할 수 있는 결론은 다음이다.

> Ring은 추상적 정보를 많이 저장하는 상자가 아니다. 이 세 토이모델에서는 선택한 함수와 section을 더하고 곱하고, relation을 quotient로 강제하고, localization으로 다시 chart를 꺼내는 실제 계산 장치였다.

그보다 큰 일반론은 다음 토이 계산이 끝난 뒤에 말해야 한다.
