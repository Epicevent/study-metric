# 함수에서 공간으로

## 사전은 관찰이 아니다 — $\mathbf P^1$, $\mathbf C/\Lambda$, $\mathrm{Gr}(2,4)$에서 무엇을 고르고 왜 ring을 쓰는가

> **기준 자료.** 이 노트는 제공된 「추상대수에서 대수기하로」를 비판적으로 읽는다. 원문은
> $$
> A\longleftrightarrow\operatorname{Spec}A,
> $$
> prime ideal은 점, ideal은 닫힌 부분공간, localization은 열린 부분, tensor product는 fiber product라는 번역표를 만들고, 이를 $S^2\simeq\mathbf P^1$과 $\mathbf C/\Lambda\simeq E$에 연결한다.
>
> **비판의 핵심.** 그 사전은 대체로 옳다. 그러나 **번역표를 안다는 것과 실제로 공간을 ring으로 관찰한다는 것은 다르다.** 가장 중요한 중간 문제,
> $$
> \boxed{\text{어떤 함수·section을 왜 골라야 하는가?}}
> $$
> 가 생략되어 있기 때문이다.
>
> **교정.** ring이 마지막에 등장하는 것이 아니다. ring은 처음부터 “허용된 함수들과 그 연산”으로 존재한다. 마지막에 등장하는 것은 선택한 관측량들이 만드는 image의 **defining ideal**이다.

이 노트는 원문을 대체하지 않는다. 원문이 제공한 사전을 실제 관찰 절차로 바꾸기 위해 필요한 비판과 보충 계산을 적는다.

각 절의 표시는 다음 뜻이다.

- **[원문]** 기준 자료가 직접 말한 내용.
- **[비판]** 원문의 논리적 빈틈에 대한 평가.
- **[보충]** 원문에는 없지만 그 빈틈을 닫기 위해 이 노트가 추가하는 계산.

---

# I. 원문이 실제로 한 일

## 1. 대수와 기하의 사전을 만든다 — [원문]

원문은 다음 대응을 기본 골격으로 삼는다.

| 대수 | 기하 |
|---|---|
| 가환환 $A$ | affine scheme $\operatorname{Spec}A$ |
| prime ideal $\mathfrak p$ | scheme-theoretic point |
| maximal ideal $\mathfrak m$ | 고전적인 닫힌 점 |
| ideal $I$ | 닫힌 부분공간 $V(I)$ |
| $f\in A$ | 공간 위의 함수 |
| localization $A_f$ | $f\neq0$인 열린 부분 $D(f)$ |
| local ring $A_{\mathfrak p}$ | 한 점 근방의 함수 germ |
| ring map $A\to B$ | 반대 방향의 사상 $\operatorname{Spec}B\to\operatorname{Spec}A$ |
| tensor product $A\otimes_RB$ | fiber product |
| field $K$ | 한 점짜리 scheme $\operatorname{Spec}K$ |
| field extension $K\subset L$ | $\operatorname{Spec}L\to\operatorname{Spec}K$ |

이 번역표는 단순 암기표가 아니다. 예를 들어

$$
A\longrightarrow A/I
$$

는 방정식을 강제하여 닫힌 부분공간을 만들고,

$$
A\longrightarrow A_f=A[1/f]
$$

는 $f=0$인 부분을 버리고 열린 부분만 보며,

$$
A\otimes_RB
$$

는 같은 base 위에서 두 공간을 pullback하는 연산이다.

## 2. $\mathbf P^1$을 두 affine chart로 붙인다 — [원문]

원문은

$$
\mathbf P^1=\operatorname{Proj}\mathbf C[X_0,X_1]
$$

이라고 쓴 뒤

$$
U_0\simeq\operatorname{Spec}\mathbf C[t],
\qquad
U_1\simeq\operatorname{Spec}\mathbf C[s],
$$

$$
s=t^{-1}
$$

로 두 affine line을 붙인다.

복소다양체에서는 두 복소 chart를 holomorphic transition map으로 붙이고, scheme에서는 affine schemes를 localization으로 얻은 overlap 위에서 붙인다는 대조가 나온다.

## 3. analytic torus를 algebraic cubic으로 바꾼다 — [원문]

원문은 analytic quotient

$$
E_{\mathrm{an}}=\mathbf C/\Lambda
$$

에서 주기적인 meromorphic functions

$$
\wp(z),\qquad \wp'(z)
$$

를 잡고

$$
\bigl(\wp'(z)\bigr)^2
=4\wp(z)^3-g_2\wp(z)-g_3
$$

을 이용한다.

따라서

$$
x=\wp(z),\qquad y=\wp'(z)
$$

로 놓으면 affine cubic

$$
y^2=4x^3-g_2x-g_3
$$

이 나타나고, 이를 homogenize하여

$$
Y^2Z=4X^3-g_2XZ^2-g_3Z^3
$$

인 projective elliptic curve를 만든다.

## 4. 최종 철학 — [원문]

원문의 결론은 다음 흐름이다.

$$
\text{다항식}
\to
\text{방정식}
\to
\text{기하적 공간},
$$

$$
\text{방정식들의 집합}
\to
\text{ideal},
$$

$$
A\to\operatorname{Spec}A,
\qquad
A_f\to\text{열린 부분},
\qquad
A_{\mathfrak p}\to\text{한 점 근방},
$$

그리고

$$
S^2
\longleftrightarrow
\mathbf C\cup\{\infty\}
\longleftrightarrow
\mathbf P^1(\mathbf C),
$$

$$
S^1\times S^1
\longleftrightarrow
\mathbf C/\Lambda
\longleftrightarrow
E(\mathbf C).
$$

여기까지는 좋은 사전이다.

---

# II. 첫 번째 비판: 사전은 관찰 프로토콜이 아니다

## 5. “$A$가 공간이다”에서 정작 $A$를 누가 고르는가

### [비판]

원문은

$$
A\longmapsto\operatorname{Spec}A
$$

를 자세히 설명한다. 그러나 우리가 이미 알고 있는 기하적 대상 $X$에서 출발할 때는 반대 방향의 문제가 먼저 생긴다.

$$
\boxed{
X\text{가 주어졌을 때 어떤 }A\text{를 고를 것인가?}
}
$$

예를 들어 compact Riemann surface $X$의 global holomorphic functions만 모으면

$$
H^0(X,\mathcal O_X)=\mathbf C
$$

인 경우가 많다. 이 ring 하나만 보면 $\mathbf P^1$, elliptic curve, genus-$2$ curve를 구별하지 못한다.

따라서 “공간 위의 함수 ring을 본다”는 말에는 최소한 다음 선택이 숨어 있다.

1. holomorphic functions를 볼 것인가, meromorphic functions를 볼 것인가?
2. pole을 허용한다면 어디에서 몇 차까지 허용할 것인가?
3. 모든 함수를 볼 것인가, 유한차원 부분공간을 고를 것인가?
4. affine coordinates를 원하는가, projective coordinates를 원하는가?
5. 한 공간만 보는가, base가 움직이는 family까지 보려는가?

이 선택을 설명하지 않으면

> ring은 공간이다

라는 말은 결과를 알고 난 뒤 붙이는 표어에 머문다.

## 6. 번역표와 관찰의 차이

다음 문장들은 번역표다.

$$
A/I\leftrightarrow V(I),
\qquad
A_f\leftrightarrow D(f),
\qquad
A\otimes_RB\leftrightarrow X\times_SY.
$$

하지만 실제 관찰은 다음 질문에 답해야 한다.

- 어떤 $I$를 왜 택하는가?
- 어떤 $f$를 invert해야 원하는 chart가 나오는가?
- 어떤 base $R$ 위에서 두 대상을 비교하는가?
- 선택한 함수들이 점을 구별하는가?
- tangent direction까지 구별하는가?

즉

$$
\boxed{
\text{대수 연산의 기하적 뜻을 아는 것}
\neq
\text{관측량을 선택할 줄 아는 것}.
}
$$

---

# III. 두 번째 비판: ring은 kernel을 위해서만 존재하지 않는다

## 7. kernel 관점이 맞는 범위

선택한 함수

$$
f_0,\dots,f_N
$$

가 있을 때 formal variables를 놓고

$$
\Phi:\mathbf C[Z_0,\dots,Z_N]
\longrightarrow
\mathcal A,
\qquad
Z_i\longmapsto f_i
$$

를 만든다.

그 kernel

$$
I=\ker\Phi
$$

은 함수들 사이의 모든 polynomial relations를 모은다. 그리고

$$
\mathbf C[Z_0,\dots,Z_N]/I
$$

는 image의 coordinate ring이 된다.

이것은 정확하다.

## 8. 그러나 ring이 마지막에 등장하는 것은 아니다

### [비판]

ring을 생각하는 이유를

> kernel로 원하는 공간을 만들기 위해서

라고만 말하면 너무 좁다.

관측량 $f_i$들을 더하고 곱할 수 있는 순간 이미 함수의 ring이 있다. 마지막에 계산하는 것은 ring 자체가 아니라

$$
\boxed{\text{image의 defining ideal }I}
$$

이다.

더구나 대수기하에서 ring은 relation 이외의 기하 연산도 수행한다.

| ring 연산 | 기하 연산 |
|---|---|
| $A/I$ | 닫힌 부분공간을 자른다 |
| $A_f$ | $f\neq0$인 열린 부분을 본다 |
| $A_{\mathfrak p}$ | 한 점 근방으로 확대한다 |
| $A\otimes_RB$ | fiber product/base change를 만든다 |
| graded ring의 $\operatorname{Proj}$ | projective 공간을 만든다 |
| sheaf of rings | local 함수들의 restriction과 gluing을 기록한다 |

따라서 수정된 문장은 이렇다.

$$
\boxed{
\text{ring은 허용된 관측량과 그 연산의 전체 체계이고,
defining ideal은 image를 자르는 마지막 출력이다.}
}
$$

---

# IV. $\mathbf P^1$: ring 연산이 실제 chart를 만드는 계산

## 9. $\operatorname{Proj}$에서 affine chart를 꺼낸다 — [보충]

$$
S=\mathbf C[X_0,X_1]
$$

를 degree-$1$ generators를 가진 graded ring으로 둔다.

$$
\mathbf P^1=\operatorname{Proj}S.
$$

$X_0\neq0$인 부분을 보려면 $X_0$를 invert한다.

$$
S_{X_0}=\mathbf C[X_0,X_0^{-1},X_1].
$$

projective scaling을 제거하려면 degree-$0$ 부분을 취한다.

$$
(S_{X_0})_0
=
\mathbf C\left[\frac{X_1}{X_0}\right]
=
\mathbf C[z].
$$

따라서

$$
D_+(X_0)\simeq\operatorname{Spec}\mathbf C[z].
$$

마찬가지로

$$
D_+(X_1)\simeq\operatorname{Spec}\mathbf C[w],
\qquad
w=\frac{X_0}{X_1}.
$$

overlap에서는

$$
z\neq0,
\qquad
w=z^{-1},
$$

$$
\mathbf C[z,z^{-1}]\simeq\mathbf C[w,w^{-1}].
$$

이 계산에서 localization은 단순한 용어가 아니다. 실제로

- 어느 chart를 보는지 결정하고,
- 그 chart에서 쓸 수 있는 함수들을 만들고,
- overlap의 transition map을 ring map으로 기록한다.

## 10. 왜 $X^2,XY,Y^2$를 고르는가 — [보충]

$\mathcal O(2)$의 complete section system은

$$
H^0(\mathbf P^1,\mathcal O(2))
=
\operatorname{span}\{X^2,XY,Y^2\}
$$

이다.

이 세 함수를 고른 이유는 relation을 이미 알고 있기 때문이 아니다.

### 10.1 같은 weight

$$
(X,Y)\mapsto(\lambda X,\lambda Y)
$$

아래 세 함수가 모두

$$
\lambda^2
$$

만큼 변하므로 projective point

$$
[X^2:XY:Y^2]
$$

가 잘 정의된다.

### 10.2 complete system이 점을 분리한다

$X\neq0$이면

$$
\frac{XY}{X^2}=\frac YX
$$

로 원래 affine coordinate를 회수한다.

반대로 $X^2,Y^2$ 두 개만 쓰면

$$
[X:Y]
\quad\text{와}\quad
[X:-Y]
$$

를 구별하지 못한다. $XY$가 빠진 정보를 보충한다.

즉 section 선택의 첫 검문은

$$
\boxed{\text{서로 다른 점을 실제로 구별하는가?}}
$$

이다.

## 11. relation은 마지막에 계산한다

$X\neq0$ chart에서

$$
[X^2:XY:Y^2]=[1:z:z^2].
$$

formal coordinates를 $[Z_0:Z_1:Z_2]$라 하면

$$
Z_0Z_2-Z_1^2=0.
$$

이제야

$$
\mathbf C[Z_0,Z_1,Z_2]
\longrightarrow
\mathbf C[X,Y]
$$

의 kernel을 계산하여

$$
I=(Z_0Z_2-Z_1^2)
$$

라고 말한다.

정확한 순서는

$$
\boxed{
\text{line bundle}
\to
\text{complete section system}
\to
\text{point separation}
\to
\text{projective map}
\to
\text{defining ideal}
}
$$

이다.

---

# V. elliptic curve: 원문이 가장 중요한 선택을 생략한 곳

## 12. 원문의 도약 — [비판]

원문은

> 적절한 함수 $x(z),y(z)$를 선택하면 cubic equation이 나타난다

고 한 뒤

$$
x=\wp(z),
\qquad
y=\wp'(z)
$$

를 제시한다.

그러나 진짜 질문은 그 사이에 있다.

$$
\boxed{
\text{왜 }\wp\text{와 }\wp'\text{인가?}
}
$$

- 왜 periodic function 하나로는 부족한가?
- 왜 derivative를 추가하는가?
- 왜 relation의 차수가 $3$인가?
- 이 함수들이 torus의 점을 충분히 구별하는가?

이 질문을 생략하면 analytic quotient가 갑자기 cubic equation으로 변한 것처럼 보인다.

## 13. pole을 허용하는 방식이 함수 선택을 조직한다 — [보충]

원점을

$$
O=0\pmod\Lambda
$$

라고 하자.

$O$에서만 pole을 허용하고 그 차수를 $n$ 이하로 제한한 meromorphic functions를

$$
L(nO)
=
\{f:\operatorname{div}(f)+nO\ge0\}\cup\{0\}
$$

라고 쓰자.

이것은 line bundle

$$
\mathcal O(nO)
$$

의 global section space와 같다.

핵심은 “meromorphic functions 전체”를 무작정 보는 것이 아니라

$$
\boxed{\text{허용할 pole의 위치와 차수를 먼저 고정한다}}
$$

는 점이다.

## 14. $L(O)$, $L(2O)$, $L(3O)$를 손으로 본다 — [보충]

Weierstrass function의 Laurent expansion은

$$
\wp(z)=\frac1{z^2}+O(z^2),
$$

$$
\wp'(z)=-\frac2{z^3}+O(z).
$$

### 14.1 simple pole 하나만 가진 elliptic function은 없다

compact Riemann surface에서 meromorphic $1$-form의 residue 합은 $0$이다. 함수가 오직 $O$에서 simple pole 하나만 가지면 그 residue를 상쇄할 다른 pole이 없으므로 불가능하다.

따라서 $L(O)$에는 상수만 있다.

$$
L(O)=\operatorname{span}\{1\}.
$$

### 14.2 double pole까지 허용하면 $\wp$가 나온다

$f\in L(2O)$의 Laurent expansion에서 $z^{-2}$ coefficient를 $\wp$의 배수로 제거하면 남은 함수는 simple pole 이하만 가진다. 위의 사실에 의해 남은 것은 상수다.

따라서

$$
L(2O)=\operatorname{span}\{1,\wp\}.
$$

### 14.3 triple pole까지 허용하면 $\wp'$가 추가된다

$f\in L(3O)$에서 $z^{-3}$ 항을 $\wp'$의 배수로 제거하고, $z^{-2}$ 항을 $\wp$의 배수로 제거하면 상수만 남는다.

따라서

$$
\boxed{
L(3O)=\operatorname{span}\{1,\wp,\wp'\}.
}
$$

이제 $1,\wp,\wp'$는 신비한 선택이 아니다. **원점에서 triple pole까지 허용한 complete section system**이다.

## 15. 왜 $\wp$ 하나로는 부족한가 — [보충]

Weierstrass function은 even이다.

$$
\wp(-z)=\wp(z).
$$

따라서

$$
z\quad\text{와}\quad -z
$$

를 구별하지 못한다. 즉

$$
E\to\mathbf P^1,
\qquad
z\mapsto[1:\wp(z)]
$$

는 일반적으로 $2$대$1$이다.

반면

$$
\wp'(-z)=-\wp'(z)
$$

이므로 $\wp'$를 추가하면 이 부호 정보가 살아난다.

$$
\Phi_{|3O|}:E\longrightarrow\mathbf P^2,
\qquad
z\longmapsto[1:\wp(z):\wp'(z)].
$$

$2$-torsion point에서는 $z=-z$가 이미 같은 점이고 $\wp'(z)=0$이므로 모순이 없다.

따라서 $\wp'$는 임의의 장식이 아니라

$$
\boxed{\wp\text{가 잃어버린 }z\leftrightarrow -z\text{ 정보를 복원한다}.}
$$

## 16. 왜 cubic relation인가 — [보충]

pole order를 비교한다.

$$
\operatorname{ord}_O(\wp)=-2,
\qquad
\operatorname{ord}_O(\wp')=-3.
$$

따라서 처음으로 같은 최대 pole order를 갖는 두 비선형 monomial은

$$
(\wp')^2,
\qquad
\wp^3
$$

이고 둘 다 order $-6$이다.

Laurent expansion의 leading term은

$$
(\wp')^2\sim\frac4{z^6},
\qquad
4\wp^3\sim\frac4{z^6}
$$

이므로 leading pole이 상쇄된다.

더 정밀한 expansion을 사용하면

$$
(\wp')^2-4\wp^3+g_2\wp+g_3
$$

는 pole이 전혀 없는 elliptic function이 된다. compact torus 위의 holomorphic function은 상수이고, Laurent expansion의 constant까지 맞추면 그 상수는 $0$이다.

따라서

$$
\boxed{
(\wp')^2=4\wp^3-g_2\wp-g_3.
}
$$

즉 cubic relation은 갑자기 주어진 것이 아니다.

$$
\boxed{
\text{선택한 section들의 pole order가 가능한 relation의 차수를 예고한다.}
}
$$

## 17. 원문에서 빠진 line bundle 문장

원문의 torus 예를 line-bundle 언어로 다시 쓰면

$$
E=\mathbf C/\Lambda,
\qquad
L=\mathcal O(3O),
$$

$$
H^0(E,L)=\operatorname{span}\{1,\wp,\wp'\},
$$

$$
E\hookrightarrow\mathbf P(H^0(E,L)^*)\simeq\mathbf P^2.
$$

이 한 줄이 들어가야

- 왜 그 함수들을 골랐는지,
- 왜 유한 개가 나오는지,
- 왜 projective embedding이 되는지

가 한 구조로 묶인다.

---

# VI. Grassmannian: projector가 있는데도 Plücker를 고르는 이유

## 18. 세 표현은 정보량으로 경쟁하지 않는다 — [보충]

$W\in\mathrm{Gr}(2,4)$를 세 방식으로 볼 수 있다.

### RREF

한 minor가 nonzero인 chart에서

$$
A\sim
\begin{pmatrix}
1&0&a&b\\
0&1&c&d
\end{pmatrix}.
$$

이는 local holomorphic/algebraic coordinates다.

### orthogonal projector

column frame $M$에 대해

$$
P_W=M(M^\dagger M)^{-1}M^\dagger.
$$

이는 frame change를 완전히 제거하고

$$
W=\operatorname{Im}P_W
$$

로 평면을 복원한다. global Hermitian·metric model이다.

### Plücker

basis $r_1,r_2$에서

$$
\omega_W=r_1\wedge r_2
$$

를 만들고

$$
W\mapsto[\omega_W]
\in\mathbf P(\bigwedge^2\mathbf C^4)
$$

로 보낸다. 역시

$$
W=\{v:v\wedge\omega_W=0\}
$$

로 평면을 복원한다.

따라서 projector와 Plücker는 점 수준에서 같은 정보를 담는다.

## 19. 차이는 family와 base change에서 선명해진다 — [비판과 보충]

projector는

- Hermitian inner product,
- adjoint,
- complex conjugation,
- orthogonal complement

을 사용한다.

반면 Plücker는

- exterior power,
- determinant,
- homogeneous polynomial,
- projective bundle

만 사용한다.

그래서 Plücker construction은 field extension이나 base change 아래 같은 공식으로 움직인다.

$$
\mathrm{Gr}(k,n)_L
\simeq
\mathrm{Gr}(k,n)_K\times_{\operatorname{Spec}K}\operatorname{Spec}L.
$$

maximal minor와 Plücker relations는 coefficient를 $K$에서 $L$로 바꾸어도 그대로다.

projector는 한 Hermitian fiber의 differential geometry에는 탁월하지만, metric이 없는 arbitrary base scheme 위 family를 정의하는 기본 장치는 아니다.

따라서 더 정확한 비교는

$$
\boxed{
\begin{array}{ccl}
\text{projector} &:& \text{한 Hermitian family의 metric geometry},\\
\text{Plücker} &:& \text{base change를 견디는 algebraic family geometry}.
\end{array}}
$$

이다.

## 20. maximal minor는 왜 선택되는가 — [보충]

$$
\omega_W=r_1\wedge r_2
$$

를 standard basis로 전개하면

$$
\omega_W
=
\sum_{i<j}p_{ij}e_i\wedge e_j.
$$

coefficient $p_{ij}$가 정확히 maximal minor다.

$$
\boxed{
\text{maximal minors}=\text{전체 top wedge의 basis coordinates}.
}
$$

일부 minor만 골라 관계를 찾는 것이 아니라, 먼저 wedge vector 전체를 기록한다. relation은 그 전체 vector가 decomposable하다는 image condition에서 나온다.

$$
\omega\wedge\omega=0
$$

을 전개하면

$$
\boxed{
p_{12}p_{34}-p_{13}p_{24}+p_{14}p_{23}=0.}
$$

그 뒤에야 polynomial ring map의 kernel이라고 압축한다.

---

# VII. Relative Grassmannian: 왜 ring 언어가 실제로 필요해지는가

## 21. 한 점이 아니라 움직이는 family를 본다

$E\to X$가 rank-$N$ algebraic vector bundle이면

$$
\mathrm{Gr}_X(k,E)
=
\{(x,W):W\subset E_x,\ \dim W=k\}
$$

를 만든다.

한 open set에서 $E$를 trivialize하면

$$
\mathrm{Gr}_X(k,E)|_U
\simeq
U\times\mathrm{Gr}(k,N).
$$

이 local pieces를 transition functions로 붙여야 한다.

## 22. relative Plücker는 projectivity를 만든다

각 fiber에서

$$
\bigwedge^kW\subset\bigwedge^kE_x
$$

는 한 차원 line이다. 따라서

$$
\mathrm{Gr}_X(k,E)
\hookrightarrow
\mathbf P_X(\bigwedge^kE)
$$

를 얻는다.

fiber마다 ordinary Plücker relations가 성립하므로 image는 projective bundle 안의 closed algebraic subvariety다.

이로부터

$$
\mathrm{Gr}_X(k,E)\to X
$$

가 projective morphism임이 나온다.

여기서 ring과 sheaf language는 단순 relation 계산보다 더 많은 일을 한다.

- local trivializations를 붙인다.
- homogeneous equations를 base 위에서 조직한다.
- base change 아래 fiber를 추적한다.
- projective morphism을 보장한다.

## 23. Reineke tower에서 반복된다

$$
X_r=\mathrm{Gr}_{X_{r-1}}(d_r,E_r)
$$

를 반복하면 각 단계가 relative Grassmannian이다.

따라서

$$
\boxed{
\text{Reineke tower}
=
\text{fiberwise Plücker varieties의 algebraic iteration}
}
$$

이다.

이 단계에서는 projector만으로는 부족하다. tower의 algebraicity와 projectivity를 base 전체에 걸쳐 말해야 하기 때문이다. 이후 Hermitian metric과 curvature를 넣을 때 projector와 Plücker metric이 다시 만난다.

---

# VIII. 세 토이모델의 공통 구조

## 24. 비교표

| 대상 | 먼저 주어진 기하 | 선택을 조직하는 것 | complete observables | 잃기 쉬운 정보 | relation |
|---|---|---|---|---|---|
| $\mathbf P^1$ | projective quotient | $\mathcal O(2)$ | $X^2,XY,Y^2$ | $XY$를 버리면 $[X:Y]\sim[X:-Y]$ | conic quadratic |
| $E=\mathbf C/\Lambda$ | analytic quotient | $\mathcal O(3O)$ | $1,\wp,\wp'$ | $\wp$만 보면 $z\sim-z$ | Weierstrass cubic |
| $\mathrm{Gr}(2,4)$ | subspace quotient | $\det\mathcal S^*$ | all maximal minors | 일부 minor만 보면 global wedge를 잃음 | Plücker quadratic |

## 25. 공통 절차

세 예를 관통하는 실제 관찰 프로토콜은 다음이다.

### 1단계 — 이미 알고 있는 기하적 대상을 고정한다

$$
X.
$$

### 2단계 — 무엇을 관찰하려는지 정한다

- local holomorphic geometry?
- metric geometry?
- projective algebraic geometry?
- family와 base change?

### 3단계 — 허용할 함수·section을 조직한다

$$
L\to X,
\qquad
V\subset H^0(X,L).
$$

### 4단계 — complete system인지 검사한다

- base point가 없는가?
- 점을 분리하는가?
- tangent direction을 분리하는가?

### 5단계 — map을 만든다

$$
\Phi_V:X\to\mathbf P(V^*).
$$

### 6단계 — image condition을 기하적으로 찾는다

- rank-one symmetric tensor?
- elliptic pole-order relation?
- decomposable wedge?

### 7단계 — 마지막에 defining ideal을 계산한다

$$
I(\Phi_V(X))
=
\ker\bigl(\operatorname{Sym}V\to R(X,L)\bigr).
$$

### 8단계 — localization, gluing, base change를 수행한다

관계를 찾았다고 끝이 아니다. chart, local ring, family, fiber를 ring operations로 계속 관찰한다.

---

# IX. 원문에 대한 최종 평가

## 26. 무엇이 좋은가

「추상대수에서 대수기하로」는 다음 역할을 잘한다.

1. abstract algebra의 연산을 geometric operations로 번역한다.
2. $\mathbf P^1$을 affine chart gluing으로 보여 준다.
3. $\mathbf C/\Lambda$가 cubic curve가 되는 큰 그림을 보여 준다.
4. quotient, localization, tensor product를 서로 다른 기하 연산으로 구분한다.
5. higher-dimensional torus에서는 polarization이 추가로 필요하다는 방향을 제시한다.

즉 훌륭한 **지도**다.

## 27. 무엇이 부족한가

그러나 지도만으로는 다음 질문에 답하지 못한다.

1. 주어진 기하에서 어느 function ring을 고르는가?
2. meromorphic pole을 어디까지 허용할 것인가?
3. 왜 그 line bundle인가?
4. 왜 그 section들을 전부 고르는가?
5. 선택한 sections가 점과 tangent를 실제로 분리하는가?
6. 왜 relation의 degree가 그렇게 되는가?
7. 왜 한 공간의 계산이 family와 base change로 확장되는가?

특히

$$
\mathbf C/\Lambda
\longrightarrow
(\wp,\wp')
\longrightarrow
\text{cubic}
$$

사이와

$$
\mathrm{Gr}(2,4)
\longrightarrow
\text{all maximal minors}
\longrightarrow
\text{Plücker relation}
$$

사이에 있는 **관측량 선택 문제**가 핵심이다.

## 28. 한 문장으로 고친다

원문의 슬로건

> geometry from functions

을 실제 계산 가능한 문장으로 고치면 다음과 같다.

$$
\boxed{
\text{기하적 목적에 맞는 line bundle과 complete section system을 고르고,
그 함수들의 연산·접합·관계를 ring으로 조직하여 공간을 다시 만든다.}
}
$$

---

# X. 현재 노트에 대한 자기비판

## 29. “ring은 맨 마지막”이라는 표현을 폐기한다

이전 노트에서

> ring은 맨 마지막에 등장한다

고 썼다면 틀린 표현이다.

정확히는

$$
\boxed{
\text{ring은 함수들을 더하고 곱하는 순간부터 있고,
마지막에 계산되는 것은 defining ideal이다.}
}
$$

## 30. projector–Plücker 비교도 한 단계 깊어져야 한다

단순히

- projector에는 $\bar z$가 들어가 holomorphic하지 않다,
- Plücker는 polynomial이다

라고 말하는 것으로는 부족하다.

더 중요한 차이는

$$
\boxed{
\text{projector는 Hermitian metric에 의존하는 한 fiber의 모델이고,
Plücker는 base change와 relative family를 견디는 algebraic construction이다.}
}
$$

이다.

## 31. kernel보다 앞선 질문을 멈추지 않는다

앞으로 어떤 embedding을 보든 먼저 묻는다.

1. 이 함수·section은 어디서 왔는가?
2. 다른 선택은 무엇인가?
3. 일부만 쓰면 어떤 점들이 합쳐지는가?
4. complete system이라는 것을 어떻게 확인하는가?
5. 이 선택은 metric, basis, chart, field 중 무엇에 의존하는가?
6. relation은 intrinsic image condition에서 어떻게 예고되는가?

이 질문에 답하지 않은 채 kernel 계산으로 넘어가면, 계산은 맞아도 이해는 후일로 밀린다.

---

# XI. 손계산 문제

## 문제 1 — $\mathbf P^1$의 affine chart를 localization으로 복원하기

$$
S=\mathbf C[X_0,X_1]
$$

에서

$$
(S_{X_0})_0
=
\mathbf C[X_1/X_0]
$$

를 degree를 직접 추적하여 보여라.

## 문제 2 — incomplete Veronese system

$$
[X:Y]\mapsto[X^2:Y^2]
$$

가 generically $2$대$1$임을 보이고, $XY$를 추가하면 $X\neq0$ chart에서 $Y/X$를 복원할 수 있음을 보여라.

## 문제 3 — simple pole 하나의 불가능성

compact Riemann surface 위 meromorphic function이 오직 한 점에서 simple pole 하나만 가질 수 없음을 residue theorem으로 설명하여라.

## 문제 4 — $L(2O)$의 basis

$\wp(z)=z^{-2}+O(z^2)$를 사용하여

$$
L(2O)=\operatorname{span}\{1,\wp\}
$$

를 Laurent coefficient 제거 방식으로 보여라.

## 문제 5 — $L(3O)$의 basis

$$
\wp'(z)=-2z^{-3}+O(z)
$$

를 사용하여

$$
L(3O)=\operatorname{span}\{1,\wp,\wp'\}
$$

를 보여라.

## 문제 6 — $\wp$가 잃는 정보

$$
\wp(-z)=\wp(z),
\qquad
\wp'(-z)=-\wp'(z)
$$

를 이용해 $\wp$ 하나와 $(\wp,\wp')$ 쌍의 point-separation 차이를 설명하여라.

## 문제 7 — cubic degree의 예고

$$
\operatorname{ord}_O(\wp)=-2,
\qquad
\operatorname{ord}_O(\wp')=-3
$$

에서 $(\wp')^2$와 $\wp^3$가 처음으로 같은 leading pole order를 갖는다는 것을 확인하여라.

## 문제 8 — projector와 Plücker의 정보량

$$
W=\operatorname{Im}P_W
$$

와

$$
W=\{v:v\wedge\omega_W=0\}
$$

를 각각 증명하여 둘 다 평면을 완전히 복원함을 보여라.

## 문제 9 — base change 아래 minor

$K\subset L$인 field extension에 대해 $K$-coefficient matrix의 maximal minors를 $L$에서 다시 계산해도 같은 polynomial formula가 유지됨을 확인하여라.

## 문제 10 — quotient와 localization을 구분하기

$$
A=\mathbf C[x,y]
$$

에서

$$
A/(y-x^2)
$$

와

$$
A_x
$$

가 각각 어떤 공간을 나타내는지 그림 없이 문장과 식으로 구분하여라.

## 문제 11 — 세 예의 line bundle

다음 세 쌍에서 section system이 무엇을 관찰하는지 한 줄씩 적어라.

$$
(\mathbf P^1,\mathcal O(2)),
$$

$$
(E,\mathcal O(3O)),
$$

$$
(\mathrm{Gr}(2,4),\det\mathcal S^*).
$$

## 문제 12 — defining ideal은 왜 마지막인가

“ring은 마지막에 등장한다”와 “defining ideal은 마지막에 계산된다”의 차이를 $\mathbf P^1$의 localization과 Veronese ideal을 사용해 설명하여라.

---

# XII. 마지막 압축

「추상대수에서 대수기하로」의 사전은 틀리지 않았다. 그러나 사전만으로는 공간을 ring으로 본 것이 아니다.

실제 관찰에는 다음이 더 필요하다.

$$
\boxed{
\text{무엇을 보고 싶은가}
\to
\text{어떤 함수·section을 허용할 것인가}
\to
\text{그들이 점과 tangent를 분리하는가}
\to
\text{어떤 map과 image condition이 생기는가}
\to
\text{마지막에 defining ideal을 계산한다}.
}
$$

그리고 ring의 역할은 kernel 하나보다 넓다.

$$
\boxed{
\text{ring은 함수의 덧셈·곱셈뿐 아니라
closed/open restriction, local geometry, gluing, morphism, fiber, base change를
한 체계에서 계산하게 한다.}
}
$$

세 토이모델은 이 흐름을 각각 보여 준다.

$$
\mathbf P^1:
\quad
\mathcal O(2)
\to
(X^2,XY,Y^2)
\to
\text{conic},
$$

$$
\mathbf C/\Lambda:
\quad
\mathcal O(3O)
\to
(1,\wp,\wp')
\to
\text{cubic},
$$

$$
\mathrm{Gr}(2,4):
\quad
\det\mathcal S^*
\to
(p_{ij})
\to
\text{Plücker quadric}.
$$

이제야

> ring은 공간이다

라는 말이 표어가 아니라 작업 순서가 된다.
