# -*- coding: utf-8 -*-
"""(퇴역) inject_nav — 내비게이션은 이제 빌더에 내장되어 있다.

사이트 전체 재빌드:  python _책빌드/site.py
이 스크립트는 아무 것도 하지 않는다 (기존 습관 호환용).
"""
print("inject_nav 는 퇴역했습니다 — 내비는 빌더 내장. `python _책빌드/site.py` 를 쓰세요.")
