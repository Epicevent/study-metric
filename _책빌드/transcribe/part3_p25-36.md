<!-- PAGE 25 -->

---

## [★] C3. 일반 fiber 계산하기

$$h(q') = h(q)$$

라고 하자.

즉

$$q'k(q')^{-1} = qkq^{-1}.$$

$$u = q^{-1}q'$$

라고 둘 때, 위 식이

$$uku^{-1} = k$$

와 동치임을 보이라.

C2를 사용하면 $u \in H$이다. 따라서

$$q' = qu$$

이고

$$h^{-1}(h(q)) = qH$$

이다.

---

## [★] C4. 구체적인 fiber 하나를 매개화하기

$$q_0 = \frac{1+j}{\sqrt{2}}$$

라고 하자.

먼저

<!-- PAGE 26 -->

$$h(q_0)$$

를 계산하라.

그다음 fiber를

$$q_0 H = \left\{ \frac{1+j}{\sqrt{2}}(\cos\theta + \sin\theta k) : \theta \in \mathbb{R} \right\}$$

로 쓰고, 실제로 곱셈을 전개하여 $a, b, c, d$ 좌표로 표현하라.

---

## [★] C6. Double cover fiber와 Hopf fiber 비교하기

$SU(2) \to SO(3)$의 fiber는

$$\{q, -q\}$$

이다.

Hopf map의 fiber는

$$qH$$

이다.

다음을 확인하라.

$$\{q, -q\} \subset qH.$$

힌트:

$$-1 = \cos\pi + \sin\pi\, k \in H.$$

---

<!-- PAGE 27 -->

## 필요한 정의

$$U_q = \begin{pmatrix} a+di & c+bi \\ -c+bi & a-di \end{pmatrix}.$$

$$R_q(v) = qvq^{-1}.$$

---

## [W] D1. $U_q \in SU(2)$ 확인하기

다음을 계산하라.

$$U_q^* U_q, \qquad \det U_q.$$

단,

$$a^2 + b^2 + c^2 + d^2 = 1.$$

목표는

$$U_q^* U_q = I, \qquad \det U_q = 1$$

이다.

---

## [W] D2. 특수 quaternion의 $SU(2)$ 행렬 계산하기

다음 $q$에 대응하는 $U_q$를 구하라.

$$q = 1, \qquad q = i, \qquad q = j, \qquad q = k, \qquad q = \frac{1+j}{\sqrt{2}}.$$

---

<!-- PAGE 28 -->

## [★] D3. $R_q(k)$만 먼저 계산하기

$$R_q(k) = qkq^{-1}$$

를 계산하라.

이 문제는 A3와 같지만 여기서는 해석이 다르다.

결론:

$$R_q(k) = h(q).$$

즉 Hopf map은 $R_q$가 북극을 어디로 보내는지 보는 map이다.

---

## [★] D4. $R_q(i)$, $R_q(j)$, $R_q(k)$ 계산하기

다음을 각각 계산하라.

$$qiq^{-1}, \qquad qjq^{-1}, \qquad qkq^{-1}.$$

그 결과를 세 개의 column으로 모아 $3 \times 3$ 행렬 $R_q$를 얻어라.

검산용 답:

$$R_q = \begin{pmatrix} a^2+b^2-c^2-d^2 & -2ad+2bc & 2ac+2bd \\ 2ad+2bc & a^2-b^2+c^2-d^2 & -2ab+2cd \\ -2ac+2bd & 2ab+2cd & a^2-b^2-c^2+d^2 \end{pmatrix}.$$

---

## [★] D5. 세 번째 열과 Hopf map 비교하기

D4의 행렬에서 세 번째 열을 쓰라.

그다음 A3의 $h(q)$ 공식과 비교하라.

결론:

$$\boxed{\Pi(R_q) = R_q e_3 = h(q)}$$

이다.

<!-- PAGE 29 -->

---

## [W] D6. $k$-축 회전 계산하기

$$q(\theta) = \cos\theta + \sin\theta k$$

라고 하자.

D4의 행렬 공식에

$$a = \cos\theta, \qquad b = 0, \qquad c = 0, \qquad d = \sin\theta$$

를 대입하라.

다음 행렬을 얻어라.

$$R_{q(\theta)} = \begin{pmatrix} \cos 2\theta & -\sin 2\theta & 0 \\ \sin 2\theta & \cos 2\theta & 0 \\ 0 & 0 & 1 \end{pmatrix}.$$

이 계산이 말하는 것:

$$q = \cos\theta + \sin\theta k$$

는 $SO(3)$에서 $k$-축 기준 $2\theta$ 회전이다.

---

## [W] D7. $2\pi$, $4\pi$ 회전 확인하기

$$q(t) = \cos\frac{t}{2} + \sin\frac{t}{2}k$$

라고 하자.

다음 값을 계산하라.

$$q(0), \qquad q(2\pi), \qquad q(4\pi).$$

그다음 $R_{q(t)}$가 $SO(3)$에서 어떤 회전인지 확인하라.

목표 결론:

<!-- PAGE 30 -->

$$q(0) = 1, \qquad q(2\pi) = -1, \qquad q(4\pi) = 1.$$

하지만

$$R_{q(0)} = R_{q(2\pi)} = R_{q(4\pi)} = I.$$

---

## 필요한 정의

$$L_\xi(q) = q\xi, \qquad R_\xi(q) = \xi q.$$

Assertion:

$$\boxed{L_\xi(q) = q\xi, \qquad R_\xi(q) = \xi q.}$$

이 convention을 끝까지 유지한다.

---

## [★] E1. $L_i$, $L_j$, $L_k$ 좌표식 얻기

$$q = a + bi + cj + dk$$

에 대해

$$qi, \qquad qj, \qquad qk$$

를 계산하라.

그 결과를 이용해

$$L_i, \qquad L_j, \qquad L_k$$

를 $(a, b, c, d)$ 좌표의 vector field로 써라.

검산용 답:

$$L_i = -b\partial_a + a\partial_b + d\partial_c - c\partial_d,$$

<!-- PAGE 31 -->

$$L_j = -c\partial_a - d\partial_b + a\partial_c + b\partial_d,$$

$$L_k = -d\partial_a + c\partial_b - b\partial_c + a\partial_d.$$

---

## [★] E2. $R_i$, $R_j$, $R_k$ 좌표식 얻기

이번에는

$$iq, \qquad jq, \qquad kq$$

를 계산하라.

그 결과를 이용해

$$R_i, \qquad R_j, \qquad R_k$$

를 좌표식으로 써라.

검산용 답:

$$R_i = -b\partial_a + a\partial_b - d\partial_c + c\partial_d,$$

$$R_j = -c\partial_a + d\partial_b + a\partial_c - b\partial_d,$$

$$R_k = -d\partial_a - c\partial_b + b\partial_c + a\partial_d.$$

---

## [★] E3. 이 vector field들이 $S^3$에 접함을 확인하기

$$F(a, b, c, d) = a^2 + b^2 + c^2 + d^2$$

라고 하자.

다음을 계산하라.

$$L_i(F), \qquad L_j(F), \qquad L_k(F).$$

<!-- PAGE 32 -->

모두 0이 나와야 한다.

같은 방식으로

$$R_i(F), \qquad R_j(F), \qquad R_k(F)$$

도 계산하라.

---

## [★] E4. Hopf fiber 방향 확인하기

Hopf fiber 방향의 $S^1$-작용은

$$q \cdot e^{\theta k} = q(\cos\theta + \sin\theta k)$$

이다.

다음을 계산하라.

$$\left. \frac{d}{d\theta} \right|_{\theta=0} q e^{\theta k}.$$

결과가

$$qk = L_k(q)$$

임을 확인하라.

---

## [★] E5. $L_k$가 Hopf map을 바꾸지 않음을 좌표로 확인하기

A3의 좌표 공식을 사용한다.

$$x = 2ac + 2bd,$$

$$y = -2ab + 2cd,$$

<!-- PAGE 33 -->

$$z = a^2 - b^2 - c^2 + d^2.$$

E1의

$$L_k = -d\partial_a + c\partial_b - b\partial_c + a\partial_d$$

를 이용해 다음을 계산하라.

$$L_k(x), \qquad L_k(y), \qquad L_k(z).$$

모두 0이 나와야 한다.

결론:

$$dh_q(L_k) = 0.$$

즉 $L_k$는 vertical vector field이다.

---

## 필요한 정의

$$h(q) = qkq^{-1}.$$

$$L_\xi(q) = q\xi, \qquad R_\xi(q) = \xi q.$$

---

## [★★] F2. $dh_q(\delta q)$ 공식 얻기

$$h(q) = qkq^{-1}$$

를 미분하라.

$\delta q \in T_q S^3$에 대해

$$dh_q(\delta q) = \delta q\, k q^{-1} - qkq^{-1}\delta q q^{-1}$$

임을 보여라.

---

<!-- PAGE 34 -->

## [★★] F4. $L_k$가 vertical임을 다시 확인하기

F3에서 $\xi = k$를 넣어라.

$$dh_q(L_k) = 0$$

임을 보여라.

이 결과는 E5의 좌표 계산과 같은 내용이다.

---

## [★★] F5. $q = 1$에서 pushforward 계산하기

F3을 이용해 다음을 계산하라.

$$dh_1(L_i), \qquad dh_1(L_j), \qquad dh_1(L_k).$$

검산용 답:

$$dh_1(L_i) = -2j,$$

$$dh_1(L_j) = 2i,$$

$$dh_1(L_k) = 0.$$

---

## 필요한 정의

$$\alpha = \frac{i}{2} \sum_{r=1}^{2} \left( z_r d\overline{z}_r - \overline{z}_r dz_r \right)$$

이고 실좌표로는

$$\alpha = \sum_{r=1}^{2} (x_r dy_r - y_r dx_r).$$

<!-- PAGE 35 -->

Reeb vector field는

$$\alpha(R_\alpha) = 1, \qquad \iota_{R_\alpha} d\alpha = 0$$

를 만족하는 vector field이다.

---

## [★] G1. 복소식에서 실좌표식 얻기

하나의 복소좌표 $z = x + iy$에 대해

$$\frac{i}{2}(z d\overline{z} - \overline{z} dz)$$

를 계산하라.

결과가

$$x dy - y dx$$

임을 보여라.

따라서

$$\alpha = \sum_{r=1}^{2} (x_r dy_r - y_r dx_r)$$

이다.

---

## [★] G2. Fiber 위에서 $\alpha$ 계산하기

fiber 하나를

$$\theta \mapsto (e^{i\theta}, 0)$$

으로 잡는다.

즉

<!-- PAGE 36 -->

$$z_1 = e^{i\theta}, \qquad z_2 = 0.$$

실좌표로는

$$x_1 = \cos\theta, \qquad y_1 = \sin\theta.$$

$$\alpha = x_1 dy_1 - y_1 dx_1$$

를 계산하여

$$\alpha = d\theta$$

임을 보여라.

---

## [W] G3. Reeb 후보 vector field 쓰기

다음 vector field를 생각한다.

$$R_\alpha = \sum_{r=1}^{2} \left( -y_r \partial_{x_r} + x_r \partial_{y_r} \right).$$

이것이 복소좌표에서는

$$(z_1, z_2) \mapsto (e^{i\theta} z_1, e^{i\theta} z_2)$$

의 infinitesimal generator임을 확인하라.

---

## [W] G4. $\alpha(R_\alpha) = 1$ 확인하기

$$\alpha = \sum_{r=1}^{2} (x_r dy_r - y_r dx_r)$$

와 G3의 $R_\alpha$를 이용하여
