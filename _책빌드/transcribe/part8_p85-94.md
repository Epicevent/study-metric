<!-- PAGE 85 -->

(c) 복소좌표에서 $\operatorname{Tr}(\partial_z P\,\partial_{\bar z}P) = \frac{1}{D^2}$를 계산하고, $ds^2 = 2\cdot 2\operatorname{Tr}(\partial_z P\,\partial_{\bar z}P)\,dz\,d\bar z = \frac{4\,dz\,d\bar z}{D^2}$ 로 $E, F, G$ 조립 없이 바로 metric이 나옴을 확인하라.

---

## 병렬 트랙. 푸리에·복소적분 손풀기 30제

원본: 05_fourier_complex_integral (100제)에서 선별. 메인 축과 독립인 워밍업 트랙. 하루 1문제. winding form 문제(71-80)는 Hopf/Euler number 트랙과 연결됨. 난이도: [W] 웜업 (1–3줄 대입·확인) · [★] 본운동 (발표 수준 유도) · [★★] 도전 (막히면 원본 앞 번호로 후퇴)

### Part 1. 복소지수와 유한합

**[W] 1. Dirichlet kernel 닫힌형**

$z = e^{ix}$라 하자. 다음 유한합을 계산하라.

$$D_N(x) = \sum_{n=-N}^{N} e^{inx}.$$

최종 답을

$$D_N(x) = \frac{\sin\left((N+\tfrac{1}{2})x\right)}{\sin(x/2)}$$

꼴로 얻어라.

**[W] 2. $D_3(x)$ 직접 전개**

다음을 직접 전개하라.

$$D_3(x) = \sum_{n=-3}^{3} e^{inx}.$$

그 다음 삼각함수 꼴과 닫힌형이 일치함을 확인하라.

<!-- PAGE 86 -->

**[W] 3. Dirichlet kernel의 적분**

다음을 계산하라.

$$\int_{-\pi}^{\pi} D_N(x)\,dx.$$

**[W] 5. 감쇠된 기하급수**

$0 < r < 1$에 대해 다음을 계산하라.

$$G(r,x) = \sum_{n=1}^{\infty} r^n e^{inx}.$$

**[W] 6. 감쇠된 cos 급수**

Part 1.5의 결과에서 실수부를 취해 다음을 계산하라.

$$C(r,x) = \sum_{n=1}^{\infty} r^n \cos nx.$$

### Part 2. 로그급수와 argument

**[★] 11. 로그급수 증명**

$|z| < 1$에서 다음을 보이라.

$$-\log(1-z) = \sum_{n=1}^{\infty} \frac{z^n}{n}.$$

**[★] 12. 허수부로 sine-over-n 얻기**

$z = re^{ix},\ 0 < r < 1$를 Part 2.11에 대입하여 다음을 얻어라.

$$\sum_{n=1}^{\infty} \frac{r^n \sin nx}{n} = \operatorname{Im}\bigl[-\log(1 - re^{ix})\bigr].$$

<!-- PAGE 87 -->

**[★] 15. 세 구체값**

다음을 계산하라.

$$\sum_{n=1}^{\infty} \frac{\sin(n\pi/3)}{n}, \qquad \sum_{n=1}^{\infty} \frac{\sin(n\pi/2)}{n}, \qquad \sum_{n=1}^{\infty} \frac{\sin(3n\pi/2)}{n}.$$

### Part 3. 홀수항 추출과 사각파

**[★] 21. 홀수항 추출 공식**

다음을 정의한다.

$$F(x) = \sum_{n=1}^{\infty} \frac{\sin nx}{n}, \qquad T(x) = \sum_{k=0}^{\infty} \frac{\sin((2k+1)x)}{2k+1}.$$

다음을 보이라.

$$T(x) = F(x) - \frac{1}{2}F(2x).$$

**[★] 24. Leibniz 급수**

Part 3.22를 이용하여 다음을 보이라.

$$1 - \frac{1}{3} + \frac{1}{5} - \frac{1}{7} + \cdots = \frac{\pi}{4}.$$

### Part 4. 푸리에 계수 직접 계산

**[★] 31. 사각파의 복소 푸리에 계수**

$2\pi$-주기 함수

$$Q(x) = \begin{cases} 1, & 0 < x < \pi, \\ -1, & -\pi < x < 0 \end{cases}$$

에 대해 $c_n(Q)$를 직접 계산하라.

<!-- PAGE 88 -->

**[★] 33. 대칭 구간 indicator**

$0 < \alpha < \pi$에 대해

$$I_\alpha(x) = \mathbf{1}_{(-\alpha,\alpha)}(x)$$

를 $2\pi$-주기적으로 연장한다. $c_n(I_\alpha)$를 계산하라.

**[★] 39. 절댓값 함수**

$-\pi < x < \pi$에서 $f(x) = |x|$이고 $2\pi$-주기적으로 연장한 함수의 푸리에 급수를 계산하라.

**[★] 40. 제곱 함수**

$-\pi < x < \pi$에서 $f(x) = x^2$이고 $2\pi$-주기적으로 연장한 함수의 푸리에 급수를 계산하라.

---

### Part 5. 점프점과 수렴값

**[W] 41. 사각파의 점프점**

Part 4.31의 $Q(x)$에 대해 급수합이 $x = 0$, $x = \pi$에서 갖는 값을 계산하라.

**[★] 45. 사각파 부분합의 원점값**

사각파 급수

$$\frac{4}{\pi} \sum_{k=0}^{N} \frac{\sin((2k+1)x)}{2k+1}$$

의 $x = 0$ 값을 계산하라.

### Part 6. 복소적분으로 삼각적분 계산

**[W] 51. 원 위의 기본 적분**

정수 $m$에 대해 다음을 계산하라.

<!-- PAGE 89 -->

$$\oint_{|z|=1} z^m\,dz.$$

**[W] 52. 지수함수 직교성**

Part 6.51을 이용하여 다음을 계산하라.

$$\int_0^{2\pi} e^{in\theta}\,d\theta.$$

**[W] 53. sine/cosine 직교성**

Part 6.52를 이용하여 다음을 계산하라.

$$\int_0^{2\pi} \cos(n\theta)\,d\theta, \qquad \int_0^{2\pi} \sin(n\theta)\,d\theta.$$

**[★] 54. 표준 삼각적분**

$a > |b| > 0$일 때 다음을 residue로 계산하라.

$$\int_0^{2\pi} \frac{d\theta}{a + b\cos\theta}.$$

### Part 7. Poisson kernel과 Abel 합

**[★] 63. Poisson kernel 계수**

$$P_r(\theta) = \frac{1 - r^2}{1 - 2r\cos\theta + r^2}$$

라 하자. 다음을 계산하라.

$$\frac{1}{2\pi} \int_0^{2\pi} P_r(\theta) e^{-in\theta}\,d\theta.$$

<!-- PAGE 90 -->

**[★] 64. Poisson kernel의 급수 전개**

다음을 보이라.

$$P_r(\theta) = 1 + 2\sum_{n=1}^{\infty} r^n \cos n\theta.$$

### Part 8. winding form과 argument

**[★] 71. winding form의 polar 표현**

$\mathbb{C}^\times$에서

$$\omega = \operatorname{Im}\frac{dz}{z}$$

라 하자. $z = \rho e^{i\theta}$로 놓고 $\omega$를 계산하라.

**[W] 72. 단위원의 winding integral**

$\gamma(t) = e^{it},\ 0 \le t \le 2\pi$에 대해 다음을 계산하라.

$$\int_\gamma \omega.$$

**[W] 73. $n$번 감는 원**

$\gamma_n(t) = Re^{int},\ 0 \le t \le 2\pi$에 대해 다음을 계산하라.

$$\frac{1}{2\pi} \int_{\gamma_n} \omega.$$

**[★] 74. 원점을 감지 않는 원**

$\gamma(t) = 2 + e^{it},\ 0 \le t \le 2\pi$에 대해 다음을 계산하라.

$$\int_\gamma \omega.$$

<!-- PAGE 91 -->

**[★★] 80. $1 - e^{ix}$의 argument jump**

$x \to 0^+$, $x \to 2\pi^-$에서

$$\operatorname{Arg}(1 - e^{ix})$$

의 극한을 각각 계산하라. 이 차이가 sawtooth 급수와 어떻게 연결되는지 설명하라.

---

### Part 9. Parseval과 값 계산

**[★] 86. 사각파 Parseval**

Part 4.31의 사각파 급수를 Parseval 공식에 대입하여 다음을 유도하라.

$$\sum_{k=0}^{\infty} \frac{1}{(2k+1)^2} = \frac{\pi^2}{8}.$$

**[★★] 88. $x^2$ 급수에서 $\zeta(2)$ 얻기**

Part 4.40의 급수에 $x = \pi$를 대입하여 다음을 유도하라.

$$\sum_{n=1}^{\infty} \frac{1}{n^2} = \frac{\pi^2}{6}.$$

### Part 10. 종합 문제

**[★★] 97. 복소 로그로 사각파 재도출**

다음을 출발점으로 사각파 급수를 다시 도출하라.

$$\sum_{k=0}^{\infty} \frac{z^{2k+1}}{2k+1} = \frac{1}{2}\log\frac{1+z}{1-z}.$$

여기서 $z = re^{ix}$를 넣고 $r \to 1^-$를 취하라.

---

<!-- PAGE 92 -->

## 검산용 정답 키 (선별분만)

### Part 1

1. $D_N(x) = e^{-iNx}(1 - e^{i(2N+1)x})/(1 - e^{ix}) = \sin((N + 1/2)x)/\sin(x/2)$.
2. $D_3(x) = 1 + 2\cos x + 2\cos 2x + 2\cos 3x = \sin(7x/2)/\sin(x/2)$.
3. $2\pi$.
4. $G(r,x) = re^{ix}/(1 - re^{ix})$.
5. $C(r,x) = \dfrac{r\cos x - r^2}{1 - 2r\cos x + r^2}$.

### Part 2

11. $(1-z)^{-1} = \sum z^n$을 적분하면 된다.
12. 그대로 허수부를 취한다.
13. $\pi/3,\ \pi/4,\ -\pi/4$.

### Part 3

21. 전체항에서 짝수항 $\frac{1}{2}F(2x)$를 빼면 된다.
22. $x = \pi/2$ 대입.

### Part 4

31. $c_0 = 0$; for odd $n \ne 0$, $c_n = -2i/(\pi n)$; for even $n \ne 0$, $c_n = 0$.
32. $c_0 = \alpha/\pi$, $c_n = \sin(n\alpha)/(\pi n)$ for $n \ne 0$.
33. $|x| \sim \pi/2 - \frac{4}{\pi} \sum_{k=0}^{\infty} \cos((2k+1)x)/(2k+1)^2$.
34. $x^2 \sim \pi^2/3 + 4\sum_{n=1}^{\infty} (-1)^n \cos(nx)/n^2$.

<!-- PAGE 93 -->

### Part 5

41. 둘 다 $0$.
42. 모든 부분합이 $0$.

### Part 6

51. $2\pi i$ if $m = -1$, otherwise $0$.
52. $2\pi$ if $n = 0$, otherwise $0$.
53. $n \ne 0$이면 둘 다 $0$; $\int_0^{2\pi} \cos 0\,d\theta = 2\pi$.
54. $2\pi/\sqrt{a^2 - b^2}$.

### Part 7

63. $r^{|n|}$.
64. 계수 계산으로 확인.

### Part 8

71. $\omega = d\theta$.
72. $2\pi$.
73. $n$.
74. $0$.
75. $-\pi/2,\ \pi/2$; branch jump가 sawtooth의 불연속과 연결된다.

### Part 9

86. $1 = (8/\pi^2) \sum_{k \ge 0} 1/(2k+1)^2$.
87. $\pi^2 = \pi^2/3 + 4\sum_{n \ge 1} 1/n^2$.

<!-- PAGE 94 -->

### Part 10

97. 허수부를 취하면 $T(x) = \frac{1}{2} \operatorname{Im} \log \frac{1+e^{ix}}{1-e^{ix}}$.
