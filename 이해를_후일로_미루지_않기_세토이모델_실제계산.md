# 함수에서 공간으로

## $\mathbf P^1$, $\mathbf C/\Lambda$, $\mathrm{Gr}(2,4)$에서 ring을 실제로 쓰는 세 계산

이 노트의 질문은 하나다.

> 이미 알고 있는 기하적 대상에서 어떤 함수·section을 골라야 하며, ring은 그 선택 이후에 실제로 무엇을 계산하는가?

세 토이모델을 같은 순서로 계산한다.

1. 허용할 함수나 section을 정한다.
2. 왜 그것들을 전부 골라야 하는지 확인한다.
3. 일부를 버렸을 때 무엇이 실패하는지 본다.
4. 그 함수들이 만드는 image의 다항식 관계를 계산한다.
5. quotient와 localization으로 원래 chart를 다시 복원한다.

---

# 0. 출발점: local-to-global gluing

$S^2=U_N\cup U_S$이고 overlap 위의 closed $1$-form을 $\alpha$라 하자.

$$
d\alpha=0.
$$

partition of unity $\rho_N+\rho_S=1$을 잡고

$$
A_N=-\rho_S\alpha,
\qquad
A_S=\rho_N\alpha
$$

라고 두면 overlap에서

$$
A_S-A_N=\alpha.
$$

또

$$
dA_N=dA_S
$$

이므로 두 local $2$-form은 하나의 global $2$-form으로 붙는다.

Line bundle section에서도 같은 모양의 gluing equation이 나온다.

$$
e_\infty=g e_0,
$$

$$
s=f_0e_0=f_\infty e_\infty,
$$

따라서

$$
f_0=g f_\infty.
$$

이 계산은 local data가 overlap 조건을 통해 global object로 붙는다는 사실을 보여 준다. 그러나 `Spec`, `Proj`, section ring은 여기서 자동으로 나오지 않는다. 아래의 세 계산을 각각 별도로 닫는다.

---

# I. $\mathbf P^1$: localization, section, relation, 다시 localization

## 1. $\operatorname{Proj}$에서 affine chart를 실제로 꺼낸다

graded ring을

$$
S=\mathbf C[X,Y],
\qquad
\deg X=\deg Y=1
$$

로 둔다.

$$
\mathbf P^1=\operatorname{Proj}S.
$$

$X\neq0$인 chart를 보려면 $X$를 invert한다.

$$
S_X=\mathbf C[X,X^{-1},Y].
$$

projective chart의 coordinate ring은 $S_X$ 전체가 아니라 degree-$0$ 부분이다.

$F(X,Y)$가 degree $m$ homogeneous polynomial이면

$$
F(X,Y)=\sum_{j=0}^{m}c_jX^{m-j}Y^j.
$$

따라서

$$
\frac{F(X,Y)}{X^m}
=
\sum_{j=0}^{m}c_j\left(\frac YX\right)^j.
$$

그러므로

$$
(S_X)_0
=
\mathbf C\left[\frac YX\right].
$$

이제

$$
z=\frac YX
$$

라고 쓰면

$$
(S_X)_0=\mathbf C[z].
$$

즉

$$
D_+(X)\simeq\operatorname{Spec}\mathbf C[z].
$$

마찬가지로

$$
w=\frac XY,
$$

$$
D_+(Y)\simeq\operatorname{Spec}\mathbf C[w].
$$

overlap에서는

$$
w=z^{-1},
$$

$$
\mathbf C[z,z^{-1}]\simeq\mathbf C[w,w^{-1}].
$$

**계산 결과.** localization은 단순히 “열린 부분을 본다”는 표어가 아니다. $X\neq0$을 강제하여 ratio $Y/X$를 실제 정규함수로 만들었다.

## 2. $\mathcal O(2)$의 global sections를 gluing equation으로 전수조사한다

두 chart의 local frame을 $e_0,e_\infty$라 하고

$$
e_\infty=z^2e_0
$$

로 붙인다.

section을

$$
s=f_0(z)e_0=f_\infty(w)e_\infty
$$

라고 쓰면

$$
f_\infty(w)=w^2f_0(1/w).
$$

$U_0\simeq\mathbf C$에서 $f_0$는 entire function이므로

$$
f_0(z)=\sum_{n=0}^{\infty}a_nz^n.
$$

따라서

$$
f_\infty(w)
=
\sum_{n=0}^{\infty}a_nw^{2-n}.
$$

$w=0$에서 holomorphic이려면 음의 지수가 없어야 한다.

$$
a_n=0
\qquad
(n\ge3).
$$

결국

$$
f_0(z)=a_0+a_1z+a_2z^2.
$$

따라서

$$
H^0(\mathbf P^1,\mathcal O(2))
=
\operatorname{span}\{X^2,XY,Y^2\}.
$$

**계산 결과.** transition을 통과하는 함수가 정확히 세 방향뿐임을 확인했다.

## 3. 왜 세 section을 전부 고르는가

두 section만 사용한 map을 보자.

$$
[X:Y]\longmapsto[X^2:Y^2].
$$

그러면

$$
[X:Y]
\quad\text{와}\quad
[X:-Y]
$$

가 같은 image로 간다. 점을 분리하지 못한다.

세 section을 모두 쓰면

$$
\nu_2([X:Y])=[X^2:XY:Y^2].
$$

$X\neq0$인 chart에서

$$
\frac{XY}{X^2}=\frac YX=z.
$$

따라서 image에서 원래 $z$를 회수한다. $X=0$인 점은 유일하게

$$
[0:0:1]
$$

로 간다.

## 4. tangent direction도 잃지 않는지 계산한다

$X\neq0$ chart에서

$$
z\longmapsto(u,v)=(z,z^2).
$$

미분하면

$$
\frac{d(u,v)}{dz}=(1,2z).
$$

첫 성분이 항상 $1$이므로 differential은 사라지지 않는다.

무한대 chart에서는

$$
w=\frac XY,
$$

$$
[w^2:w:1]
$$

로 나타난다. affine coordinates를

$$
(\widetilde u,\widetilde v)=(w^2,w)
$$

로 잡으면

$$
\frac{d(\widetilde u,\widetilde v)}{dw}=(2w,1).
$$

$w=0$에서도 differential은 사라지지 않는다.

**계산 결과.** 세 section은 점뿐 아니라 tangent direction도 분리한다.

## 5. relation을 모르는 상태에서 kernel을 계산한다

formal coordinates를 $Z_0,Z_1,Z_2$라 하고

$$
\Phi:
\mathbf C[Z_0,Z_1,Z_2]
\longrightarrow
\mathbf C[X,Y]
$$

를

$$
Z_0\longmapsto X^2,
\qquad
Z_1\longmapsto XY,
\qquad
Z_2\longmapsto Y^2
$$

로 정의한다.

다음 relation은 바로 보인다.

$$
Z_0Z_2-Z_1^2.
$$

실제로

$$
X^2Y^2-(XY)^2=0.
$$

이 relation 외에 더 있는지 확인한다. quotient에서

$$
Z_1^2=Z_0Z_2
$$

를 사용하면 모든 monomial을

$$
Z_0^aZ_2^c
$$

또는

$$
Z_0^aZ_1Z_2^c
$$

꼴로 줄일 수 있다.

이들의 image는 각각

$$
X^{2a}Y^{2c},
$$

$$
X^{2a+1}Y^{2c+1}.
$$

서로 다른 $(a,c)$는 서로 다른 monomial로 간다. 따라서 추가 relation은 없다.

$$
\ker\Phi=(Z_0Z_2-Z_1^2).
$$

## 6. quotient ring에서 원래 affine chart를 다시 복원한다

conic의 homogeneous coordinate ring을

$$
R=
\frac{\mathbf C[Z_0,Z_1,Z_2]}
{(Z_0Z_2-Z_1^2)}
$$

라고 하자.

$Z_0\neq0$인 chart에서 degree-$0$ ratios를

$$
u=\frac{Z_1}{Z_0},
\qquad
v=\frac{Z_2}{Z_0}
$$

로 둔다. relation을 $Z_0^2$로 나누면

$$
v-u^2=0.
$$

따라서 chart ring은

$$
\frac{\mathbf C[u,v]}{(v-u^2)}
\simeq
\mathbf C[u].
$$

그리고 $u=z$다.

**계산 결과.** quotient로 conic relation을 강제하고, localization으로 원래 chart $\mathbf C[z]$를 정확히 회수했다.

---

# II. $\mathbf C/\Lambda$: pole order가 $1,\wp,\wp'$를 고른다

## 7. 무엇을 입력으로 쓰는가

격자 $\Lambda$에 대해

$$
E=\mathbf C/\Lambda
$$

를 생각한다. 다음은 입력으로 사용한다.

- $\wp$와 $\wp'$가 $\Lambda$-periodic meromorphic functions라는 사실.
- compact Riemann surface 위 meromorphic $1$-form의 residue 합이 $0$이라는 사실.
- compact Riemann surface 위 holomorphic function은 상수라는 사실.

이번 절에서 직접 계산할 것은 다음이다.

1. $L(O)$, $L(2O)$, $L(3O)$의 basis.
2. $\wp$만 사용할 때 잃는 정보.
3. Laurent expansion으로 cubic relation의 pole을 제거하는 과정.

## 8. 허용할 pole을 먼저 고정한다

원점의 class를

$$
O=0\pmod\Lambda
$$

라고 하자.

$O$에서만 pole을 허용하고 pole order가 $n$ 이하인 meromorphic functions의 공간을

$$
L(nO)
$$

라고 쓰자. 이는

$$
H^0(E,\mathcal O(nO))
$$

와 같은 대상이다.

**선택 기준.** meromorphic functions 전체를 무작정 고르는 것이 아니라, pole의 위치와 최대 차수를 먼저 고정한다.

## 9. simple pole 하나만 가진 elliptic function은 없다

$f\in L(O)$가 nonconstant라고 가정하자. 그러면 $f$는 $O$에서 simple pole을 가져야 한다.

meromorphic $1$-form

$$
f(z)\,dz
$$

는 $O$에서만 residue를 갖는다. residue 합은 $0$이어야 하므로 그 residue는 $0$이다. 그러면 simple pole이 아니다.

따라서

$$
L(O)=\operatorname{span}\{1\}.
$$

## 10. double pole까지 허용하면 $1,\wp$가 나온다

원점 근방에서

$$
\wp(z)=\frac1{z^2}+O(z^2).
$$

$f\in L(2O)$의 Laurent expansion을

$$
f(z)=a_{-2}z^{-2}+a_{-1}z^{-1}+a_0+a_1z+\cdots
$$

라고 쓰자.

residue theorem으로

$$
a_{-1}=0.
$$

이제

$$
f-a_{-2}\wp
$$

는 pole이 없다. compact Riemann surface 위 holomorphic function은 상수이므로

$$
f=a_0+a_{-2}\wp.
$$

따라서

$$
L(2O)=\operatorname{span}\{1,\wp\}.
$$

## 11. triple pole까지 허용하면 $\wp'$가 추가된다

미분하면

$$
\wp'(z)=-\frac2{z^3}+O(z).
$$

$f\in L(3O)$라 하고

$$
f(z)=a_{-3}z^{-3}+a_{-2}z^{-2}+a_{-1}z^{-1}+\cdots
$$

라고 쓰자.

residue theorem으로

$$
a_{-1}=0.
$$

먼저

$$
f+\frac{a_{-3}}2\wp'
$$

를 취하면 $z^{-3}$ 항이 사라진다. 이어서 $z^{-2}$ 항을 $\wp$의 배수로 제거하면 pole이 전부 사라진다. 남는 것은 상수다.

따라서

$$
L(3O)=\operatorname{span}\{1,\wp,\wp'\}.
$$

이제 $1,\wp,\wp'$는 갑자기 고른 세 함수가 아니다. $O$에서 triple pole까지 허용했을 때 나오는 complete section system이다.

## 12. 왜 $\wp$ 하나로는 점을 분리하지 못하는가

격자합의 대칭성에서

$$
\wp(-z)=\wp(z).
$$

따라서 $\wp$만 보면

$$
z
\quad\text{와}\quad
-z
$$

를 구별하지 못한다.

반면

$$
\wp'(-z)=-\wp'(z).
$$

따라서 $\wp'$가 부호 정보를 복원한다. $z=-z$인 $2$-torsion에서는 두 점이 애초에 같은 torus point이고 $\wp'(z)=0$이다.

## 13. cubic relation을 Laurent expansion으로 확인한다

표준 전개를 다음 차수까지 쓴다.

$$
\wp(z)
=
\frac1{z^2}
+
\frac{g_2}{20}z^2
+
\frac{g_3}{28}z^4
+
O(z^6),
$$

$$
\wp'(z)
=
-\frac2{z^3}
+
\frac{g_2}{10}z
+
\frac{g_3}{7}z^3
+
O(z^5).
$$

먼저 제곱한다.

$$
(\wp')^2
=
\frac4{z^6}
-
\frac{2g_2}{5}\frac1{z^2}
-
\frac{4g_3}{7}
+
O(z^2).
$$

다음으로 세제곱한다.

$$
4\wp^3
=
\frac4{z^6}
+
\frac{3g_2}{5}\frac1{z^2}
+
\frac{3g_3}{7}
+
O(z^2).
$$

따라서

$$
(\wp')^2-4\wp^3
=
-g_2\frac1{z^2}
-g_3
+
O(z^2).
$$

여기에 $g_2\wp+g_3$를 더하면 pole과 constant term이 사라진다.

$$
H(z)
=
(\wp')^2-4\wp^3+g_2\wp+g_3.
$$

$H$는 elliptic function이고 pole이 없다. 따라서 torus 위 holomorphic function이며 상수다. 위 전개에서 그 상수는 $0$이다.

$$
(\wp')^2=4\wp^3-g_2\wp-g_3.
$$

relation의 차수도 pole order에서 미리 보인다.

$$
\operatorname{ord}_O(\wp)=-2,
\qquad
\operatorname{ord}_O(\wp')=-3.
$$

처음으로 최대 pole order가 맞는 비선형 monomial은

$$
(\wp')^2
\quad\text{와}\quad
\wp^3
$$

이고 둘 다 pole order $6$을 가진다.

## 14. projective cubic으로 올린다

projective coordinates의 순서를

$$
[Z:X:Y]=[1:\wp:\wp']
$$

로 잡는다. 그러면 relation은

$$
Y^2Z=4X^3-g_2XZ^2-g_3Z^3.
$$

$z\to0$일 때

$$
[1:\wp(z):\wp'(z)]
\sim
[z^3:z:-2],
$$

따라서 원점 $O$는

$$
[0:0:1]
$$

로 간다. 표준 좌표순서 $[X:Y:Z]$에서는 같은 점이 $[0:1:0]$이다.

### 이 절에서 입력으로 남긴 것

다음은 여기서 새로 증명했다고 주장하지 않는다.

- $\wp$의 lattice periodicity.
- compact Riemann surface에서 divisor의 degree가 $0$이라는 정리.
- discriminant가 nonzero일 때 cubic이 smooth하다는 판정.
- 위 map이 biholomorphism이라는 전체 uniformization theorem.

---

# III. $\mathrm{Gr}(2,4)$: RREF, projector, wedge, ring

## 15. RREF chart에서 네 자유좌표를 얻는다

$W\in\mathrm{Gr}(2,4)$의 basis를 두 row로 놓으면 full-rank matrix

$$
A\in M_{2\times4}(\mathbf C)
$$

를 얻는다. basis change는

$$
A\longmapsto gA,
\qquad
g\in GL(2,\mathbf C)
$$

이다.

첫 두 열의 determinant가 nonzero인 chart에서는 Gaussian elimination으로

$$
A\sim
\begin{pmatrix}
1&0&a&b\\
0&1&c&d
\end{pmatrix}.
$$

여기서 $a,b,c,d$는 독립이다. 이 chart는 $\mathbf A^4$다. 이 네 좌표 사이에서 relation을 찾는 것이 아니다.

## 16. projector는 이미 basis ambiguity를 완전히 제거한다

row frame convention에서 orthogonal projector는

$$
P_A=A^\dagger(AA^\dagger)^{-1}A.
$$

$A\mapsto gA$를 대입하면

$$
\begin{aligned}
P_{gA}
&=A^\dagger g^\dagger
\bigl(gAA^\dagger g^\dagger\bigr)^{-1}
gA\\
&=A^\dagger g^\dagger
(g^\dagger)^{-1}(AA^\dagger)^{-1}g^{-1}gA\\
&=P_A.
\end{aligned}
$$

또

$$
\operatorname{Im}P_A=W.
$$

따라서 projector는 점을 잃지 않는다. Plücker를 쓰는 이유는 projector가 실패해서가 아니다.

그런데 RREF matrix를 $A=(I_2\ Z)$로 쓰면 projector에는 $Z^\dagger$와 $(I_2+ZZ^\dagger)^{-1}$가 들어간다. projector는 Hermitian metric 계산에는 적합하지만 $Z$의 holomorphic polynomial coordinate는 아니다.

## 17. metric 없이 평면을 기록하는 top wedge

두 row를 $r_1,r_2$라 하자.

$$
\omega_W=r_1\wedge r_2
\in
\bigwedge^2\mathbf C^4.
$$

basis를 $g$로 바꾸면

$$
\omega_W\longmapsto\det(g)\,\omega_W.
$$

따라서 projective class $[\omega_W]$는 basis와 무관하다.

또

$$
W
=
\{v\in\mathbf C^4:v\wedge\omega_W=0\}.
$$

즉 wedge도 projector와 마찬가지로 원래 평면을 완전히 복원한다.

## 18. maximal minors를 직접 전개한다

RREF chart에서

$$
r_1=e_1+ae_3+be_4,
$$

$$
r_2=e_2+ce_3+de_4.
$$

wedge를 전개하면

$$
\begin{aligned}
r_1\wedge r_2
={}&e_1\wedge e_2
+c\,e_1\wedge e_3
+d\,e_1\wedge e_4\\
&-a\,e_2\wedge e_3
-b\,e_2\wedge e_4
+(ad-bc)e_3\wedge e_4.
\end{aligned}
$$

따라서 Plücker coordinates는

$$
p_{12}=1,
$$

$$
p_{13}=c,
\qquad
p_{14}=d,
$$

$$
p_{23}=-a,
\qquad
p_{24}=-b,
$$

$$
p_{34}=ad-bc.
$$

**계산 결과.** maximal minors는 임의로 고른 함수가 아니다. top wedge 전체의 basis coefficients다.

## 19. decomposability에서 quadratic relation을 얻는다

일반 $2$-vector를

$$
\omega
=
p_{12}e_{12}+p_{13}e_{13}+p_{14}e_{14}
+p_{23}e_{23}+p_{24}e_{24}+p_{34}e_{34}
$$

라고 쓰자.

평면에서 온 vector라면

$$
\omega=r_1\wedge r_2
$$

이므로

$$
\omega\wedge\omega=0.
$$

직접 전개하면

$$
\omega\wedge\omega
=
2\bigl(
p_{12}p_{34}-p_{13}p_{24}+p_{14}p_{23}
\bigr)e_{1234}.
$$

따라서

$$
p_{12}p_{34}-p_{13}p_{24}+p_{14}p_{23}=0.
$$

## 20. local kernel을 실제로 계산한다

$p_{12}=1$ chart에서 formal coordinates를

$$
x_{13},x_{14},x_{23},x_{24},x_{34}
$$

라고 둔다.

$$
\Psi:
\mathbf C[x_{13},x_{14},x_{23},x_{24},x_{34}]
\longrightarrow
\mathbf C[a,b,c,d]
$$

를

$$
x_{13}\longmapsto c,
\quad
x_{14}\longmapsto d,
\quad
x_{23}\longmapsto-a,
\quad
x_{24}\longmapsto-b,
$$

$$
x_{34}\longmapsto ad-bc
$$

로 정의한다.

다음 polynomial은 kernel에 있다.

$$
G=x_{34}-x_{13}x_{24}+x_{14}x_{23}.
$$

임의의 polynomial을 $x_{34}$에 관한 polynomial로 보고 monic polynomial $G$로 나누면

$$
F=QG+R(x_{13},x_{14},x_{23},x_{24}).
$$

$F\in\ker\Psi$이면

$$
R(c,d,-a,-b)=0.
$$

$a,b,c,d$가 독립이므로 $R=0$이다. 따라서

$$
\ker\Psi=(G).
$$

## 21. projective quotient ring에서 RREF chart를 복원한다

homogeneous coordinate ring을

$$
R_{\mathrm{Pl}}
=
\frac{
\mathbf C[p_{12},p_{13},p_{14},p_{23},p_{24},p_{34}]
}{
(p_{12}p_{34}-p_{13}p_{24}+p_{14}p_{23})
}
$$

라고 하자.

$p_{12}\neq0$인 chart에서 degree-$0$ ratios를

$$
u_{ij}=\frac{p_{ij}}{p_{12}}
$$

로 둔다. relation을 $p_{12}^2$로 나누면

$$
u_{34}-u_{13}u_{24}+u_{14}u_{23}=0.
$$

따라서 $u_{34}$를 제거할 수 있고 chart ring은

$$
\mathbf C[u_{13},u_{14},u_{23},u_{24}].
$$

RREF 좌표와의 대응은

$$
u_{13}=c,
\qquad
u_{14}=d,
\qquad
u_{23}=-a,
\qquad
u_{24}=-b.
$$

즉 projective Plücker ring을 localization하면 처음의 자유 chart $\mathbf C[a,b,c,d]$가 정확히 돌아온다.

## 22. base change가 보존하는 것

체의 확대 $K\subset L$을 생각하자. Plücker quadratic을

$$
P=p_{12}p_{34}-p_{13}p_{24}+p_{14}p_{23}
$$

라고 쓰면

$$
\left(
\frac{K[p_{ij}]}{(P)}
\right)
\otimes_KL
\simeq
\frac{L[p_{ij}]}{(P)}.
$$

coefficient field를 바꾸어도 같은 polynomial formula가 남는다. projector는 Hermitian adjoint를 요구하지만 determinant와 wedge는 field extension과 relative family에서 같은 식으로 움직인다.

---

# IV. 세 계산에서 ring이 실제로 한 일

## 23. $\mathbf P^1$

- localization으로 $Y/X$를 정규함수로 만들었다.
- $\mathcal O(2)$의 gluing equation으로 세 section을 얻었다.
- 일부 section을 버리면 점이 합쳐짐을 확인했다.
- kernel ideal로 conic relation을 모았다.
- quotient와 localization으로 원래 chart를 복원했다.

## 24. $\mathbf C/\Lambda$

- pole의 위치와 최대 차수를 고정해 finite section space를 만들었다.
- $\wp$ 하나가 $z$와 $-z$를 합친다는 실패를 확인했다.
- $\wp'$를 추가해 빠진 부호 정보를 복원했다.
- Laurent expansion으로 cubic relation의 pole을 제거했다.

## 25. $\mathrm{Gr}(2,4)$

- RREF로 자유 affine chart를 만들었다.
- projector가 이미 평면을 완전히 기록함을 확인했다.
- top wedge를 전개하여 maximal minors의 출처를 찾았다.
- decomposability와 kernel 계산으로 Plücker relation을 얻었다.
- localization으로 다시 자유 chart를 회수했다.
- tensor product로 같은 식이 base change 아래 유지됨을 확인했다.

현재 정당하게 말할 수 있는 결론은 다음이다.

> Ring은 추상적 정보를 많이 저장하는 상자가 아니다. 이 세 토이모델에서는 선택한 함수와 section을 더하고 곱하고, relation을 quotient로 강제하고, localization으로 다시 chart를 꺼내는 실제 계산 장치였다.

## 26. 아직 남은 문제

1. 일반 line bundle에서 언제 sections가 점을 분리하는가.
2. 언제 tangent directions까지 분리하는가.
3. ample과 very ample의 차이를 토이모델에서 어떻게 관찰하는가.
4. $\mathbf C/\Lambda$의 cubic map이 전역 biholomorphism이라는 것을 어떻게 완결하는가.
5. relative Grassmannian의 local equations가 base chart 사이에서 어떻게 붙는가.
6. Reineke tower의 각 단계에서 어떤 determinant line bundle과 curvature form이 실제로 선택되는가.
